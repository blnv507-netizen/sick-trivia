import React, { useState, useEffect, useRef } from "react";

// Web storage adapter: local data stays in the browser; shared room data
// is stored by the Node backend so multiple phones can play the same room.
if (typeof window !== "undefined" && !(window as any).storage) {
  (window as any).storage = {
    async set(key: string, value: string, shared = true) {
      if (!shared) { localStorage.setItem(key, value); return; }
      const r = await fetch("/api/kv/" + encodeURIComponent(key), {
        method: "PUT", headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ value })
      });
      if (!r.ok) throw new Error("storage write failed");
    },
    async get(key: string, shared = true) {
      if (!shared) { const value = localStorage.getItem(key); return value === null ? null : { value }; }
      const r = await fetch("/api/kv/" + encodeURIComponent(key));
      if (r.status === 404) return null;
      if (!r.ok) throw new Error("storage read failed");
      return await r.json();
    },
    async delete(key: string, shared = true) {
      if (!shared) { localStorage.removeItem(key); return; }
      const r = await fetch("/api/kv/" + encodeURIComponent(key), { method: "DELETE" });
      if (!r.ok && r.status !== 404) throw new Error("storage delete failed");
    }
  };
}

/* ============================================================
   SICK TRIVIA — لعبة أسئلة جماعية بين الربع
   كل سؤال: الأخير بالترتيب يختار الفئة 😈
   إجابات كتابية والذكاء الاصطناعي يحكم ⚡ وأحداث كل 3 أسئلة 🎲
   ============================================================ */

const PTS = { 1: 200, 2: 400, 3: 600 };
const DLBL = { 1: "سهل", 2: "متوسط", 3: "صعب" };
const CATS = ["أعلام", "شكل ورسم", "مين البوس؟", "وين المكان؟", "مين قالها؟", "وش الغرض؟", "الرابط المشترك", "قبل ولا بعد؟", "إيموجي", "دليلين", "الأغرب", "لو كنت مكانك", "بوسات السولز", "لور السولز", "سولز لايك", "أوفرواتش", "أزياء الشخصيات", "ألعاب فيديو", "رياضيات", "فيزياء", "علوم", "عام", "رياضة", "أفلام ومسلسلات", "تاريخ", "جغرافيا", "تقنية"];
const TYPED_SEC = 40;
const BLIND_SHOW = 6;
const VOTE_SEC = 15;
const PICK_SEC = 25;
const START_DELAY = 5000; // بداية موحّدة — وقت كافي يوصل السؤال لكل الأجهزة
const REVEAL_SEC = 14;   // الانتقال التلقائي بعد النتيجة
const EVENT_SEC = 12;    // الانتقال التلقائي بعد شاشة الحدث

/* ---------- الأحداث ---------- */
const EVENTS = [
  { id: "double",   icon: "✖️2", name: "دبل النقاط",  desc: "هذا السؤال نقاطه مضاعفة! لا تفوّتها" },
  { id: "risk",     icon: "☠️", name: "خطر",         desc: "الجواب الغلط يخصم منك نص نقاط السؤال!" },
  { id: "blitz",    icon: "⚡", name: "برق",          desc: "نص الوقت بس! أسرع أسرع" },
  { id: "sniper",   icon: "🎯", name: "للأسرع فقط",   desc: "بس أول إجابة صحيحة تاخذ النقاط، الباقي صفر" },
  { id: "blind",    icon: "🙈", name: "سؤال أعمى",    desc: "السؤال بيختفي بعد " + BLIND_SHOW + " ثواني — احفظه زين!" },
  { id: "steal",    icon: "🦹", name: "سرقة",         desc: "الأسرع بالإجابة الصح يسرق نص النقاط من المتصدر!" },
  { id: "roulette", icon: "🎰", name: "روليت",        desc: "نقاط السؤال مجهولة — من 100 إلى 1000، تنكشف بعد الإجابات!" },
];


/* ---------- الآيتمات (تخريب) ---------- */
const ITEMS = [
  { id: "cancel",  icon: "🚫", name: "كنسل",    desc: "الهدف ما يقدر يجاوب هذا السؤال نهائيًا", target: true },
  { id: "rush",    icon: "⏩", name: "تسريع",   desc: "وقت الهدف ينص", target: true },
  { id: "fog",     icon: "🌫️", name: "ضباب",    desc: "السؤال يطلع مشوّش للهدف أول نصف الوقت", target: true },
  { id: "vanish",  icon: "👻", name: "اختفاء",  desc: "السؤال يختفي عن الهدف بعد 5 ثواني", target: true },
  { id: "lock",    icon: "🔒", name: "قفل",     desc: "الهدف ما يقدر يكتب أول 8 ثواني", target: true },
  { id: "mirror",  icon: "🪞", name: "مرايا",   desc: "السؤال يطلع معكوس للهدف", target: true },
  { id: "tax",     icon: "💸", name: "ضريبة",   desc: "لو الهدف جاوب صح، ياخذ نص النقاط بس", target: true },
  { id: "shield",  icon: "🛡️", name: "درع",     desc: "يحميك من كل آيتم بالسؤال الجاي", target: false },
];
const itemById = (id) => ITEMS.find((x) => x.id === id);

/* ---------- بنك الأسئلة ---------- */
const BANK = [
  // ===== بوسات السولز =====
  { cat: "بوسات السولز", d: 1, q: "بوس إلدن رينق الذهبي أول جدار يصدمك، صاحب جملة Foolish Ambitions؟", a: "Margit", alt: ["مارغيت"] },
  { cat: "بوسات السولز", d: 1, q: "آخر بوس في إلدن رينق، الكائن الكوني بعد رادغون؟", a: "Elden Beast", alt: ["إلدن بيست"] },
  { cat: "بوسات السولز", d: 1, q: "أول بوس يقابلك في دارك سولز 3 عند الجرس؟", a: "Gundyr", alt: ["غوندر"] },
  { cat: "بوسات السولز", d: 1, q: "بوس إلدن رينق العملاق النار اللي يتدحرج بالجبل المتجمد؟", a: "Fire Giant", alt: ["عملاق النار"] },
  { cat: "بوسات السولز", d: 1, q: "البوس النهائي في سيكيرو، السيّاف الأسطوري العجوز؟", a: "Isshin", alt: ["إيشين"] },
  { cat: "بوسات السولز", d: 2, q: "بوسة إلدن رينق صاحبة الجملة الشهيرة I am ... Blade of Miquella؟", a: "Malenia", alt: ["ماليينيا"] },
  { cat: "بوسات السولز", d: 2, q: "الصياد الأول اللي تواجهه آخر بلودبورن على الكرسي المتحرك؟", a: "Gehrman", alt: ["غيرمان"] },
  { cat: "بوسات السولز", d: 2, q: "بوسة إلدن رينق ساحرة أكاديمية رايا لوكاريا وأم رانّي؟", a: "Rennala", alt: ["رينالا"] },
  { cat: "بوسات السولز", d: 2, q: "بوس دارك سولز 3 المكوّن من أميرين أخوين، واحد يشيل الثاني؟", a: "Twin Princes", alt: ["الأمراء التوأم"] },
  { cat: "بوسات السولز", d: 2, q: "بوس سيكيرو القرد الضخم اللي يكمل القتال بدون راسه؟", a: "Guardian Ape", alt: ["القرد الحارس"] },
  { cat: "بوسات السولز", d: 2, q: "أول بوس رئيسي في Lies of P، الدمية الضخمة بمهرجان الافتتاح؟", a: "Parade Master" },
  { cat: "بوسات السولز", d: 2, q: "أول Nightlord تواجهه في Nightreign، الوحش ثلاثي الرؤوس؟", a: "Gladius", alt: ["غلاديوس"] },
  { cat: "بوسات السولز", d: 3, q: "البوس النهائي في DLC إلدن رينق، رجع بجسد جديد كقرين موعود؟", a: "Radahn", alt: ["رادان"] },
  { cat: "بوسات السولز", d: 3, q: "البوس النهائي في دارك سولز الأولى، سيد الرماد وموقد النار الأولى؟", a: "Gwyn", alt: ["غوين"] },
  { cat: "بوسات السولز", d: 3, q: "البوس السري النهائي في بلودبورن، الكائن القمري؟", a: "Moon Presence" },
  { cat: "بوسات السولز", d: 3, q: "بوس Lies of P رئيس الكنيسة الكبير اللي تواجهه بنهاية القصة؟", a: "Simon Manus", alt: ["سيمون مانوس"] },
  { cat: "بوسات السولز", d: 3, q: "ملك الدمى في Lies of P — مين تطلع هويته الحقيقية؟", a: "Romeo", alt: ["روميو"] },
  { cat: "بوسات السولز", d: 3, q: "البوس النهائي الحقيقي في Hollow Knight، الكيان المضيء؟", a: "Radiance", alt: ["رادينس"] },
  // ===== لور السولز =====
  { cat: "لور السولز", d: 1, q: "وش اسم الشجرة الذهبية العملاقة اللي تهيمن على عالم إلدن رينق؟", a: "Erdtree", alt: ["شجرة الإرد"] },
  { cat: "لور السولز", d: 1, q: "وش لقب البطل اللي تلعب فيه بإلدن رينق؟", a: "Tarnished", alt: ["التارنيشد"] },
  { cat: "لور السولز", d: 1, q: "وش لون رسالة YOU DIED الشهيرة؟", a: "أحمر" },
  { cat: "لور السولز", d: 1, q: "وش العملة اللي تجمعها وتخسرها لما تموت بدارك سولز؟", a: "Souls", alt: ["السولز"] },
  { cat: "لور السولز", d: 1, q: "وش اسم مدينة أحداث Lies of P؟", a: "Krat", alt: ["كرات"] },
  { cat: "لور السولز", d: 2, q: "من الإلهة الخالدة حاملة الرون الأكبر بإلدن رينق؟", a: "Marika", alt: ["ماريكا"] },
  { cat: "لور السولز", d: 2, q: "وش اسم المدينة المريضة اللي تدور فيها أحداث بلودبورن؟", a: "Yharnam", alt: ["يارنام"] },
  { cat: "لور السولز", d: 2, q: "وش اسم العاصمة الملكية بإلدن رينق؟", a: "Leyndell", alt: ["ليندل"] },
  { cat: "لور السولز", d: 2, q: "كم رون عظيم تحتاج كحد أدنى عشان تدخل العاصمة بإلدن رينق؟", a: "اثنين" },
  { cat: "لور السولز", d: 2, q: "وش اسم الصرصور المضيء رفيقك على الفانوس في Lies of P؟", a: "جيميني" },
  { cat: "لور السولز", d: 2, q: "وش اسم الفندق ملاذك الآمن في Lies of P؟", a: "فندق كرات" },
  { cat: "لور السولز", d: 3, q: "من والد التوأم ميكيلا وماليينيا ونصف ماريكا الثاني؟", a: "Radagon", alt: ["رادغون"] },
  { cat: "لور السولز", d: 3, q: "وش الاسم الحقيقي للدمية البطل اللي تنكشف هويته بنهاية Lies of P؟", a: "Carlo", alt: ["كارلو"] },
  { cat: "لور السولز", d: 3, q: "من أخو ماليينيا التوأم، الإمبراطور الموعود؟", a: "Miquella", alt: ["ميكيلا"] },
  { cat: "لور السولز", d: 3, q: "وش تسمى نهاية إلدن رينق اللي تختارها مع رانّي؟", a: "Age of Stars", alt: ["عصر النجوم"] },
  { cat: "لور السولز", d: 3, q: "وش اسم القصر المخفي فوق الشلال بسيكيرو موطن التنين الإلهي؟", a: "Fountainhead Palace", alt: ["قصر الينبوع"] },
  // ===== سولز لايك =====
  { cat: "سولز لايك", d: 1, q: "الاستوديو الياباني مطوّر دارك سولز وإلدن رينق؟", a: "FromSoftware", alt: ["فروم سوفتوير"] },
  { cat: "سولز لايك", d: 1, q: "المخرج الياباني الشهير أبو ألعاب السولز؟", a: "Miyazaki", alt: ["ميازاكي"] },
  { cat: "سولز لايك", d: 1, q: "لعبة السولز اللي بطلها نسخة قاتمة من بينوكيو؟", a: "Lies of P" },
  { cat: "سولز لايك", d: 1, q: "وش تسمى النار اللي ترتاح عندها وتحفظ تقدمك بدارك سولز؟", a: "Bonfire", alt: ["بونفاير"] },
  { cat: "سولز لايك", d: 1, q: "وش عملة الترقية بإلدن رينق اللي تطيح منك لما تموت؟", a: "Runes", alt: ["رونز"] },
  { cat: "سولز لايك", d: 1, q: "لعبة فروم التعاونية المنفصلة بعالم إلدن رينق اللي صدرت 2025؟", a: "Nightreign", alt: ["نايترين"] },
  { cat: "سولز لايك", d: 2, q: "لعبة فروم سوفتوير اللي بطلها نينجا بذراع صناعية؟", a: "Sekiro", alt: ["سيكيرو"] },
  { cat: "سولز لايك", d: 2, q: "لعبة The First Berserker: Khazan مبنية على عالم أي سلسلة كورية؟", a: "Dungeon and Fighter" },
  { cat: "سولز لايك", d: 2, q: "لعبة السولز الحصرية لسوني بعالم فيكتوري مرعب؟", a: "Bloodborne", alt: ["بلودبورن"] },
  { cat: "سولز لايك", d: 2, q: "وش اسم القارورة اللي تستعيد فيها صحتك بإلدن رينق؟", a: "Flask", alt: ["الفلاسك"] },
  { cat: "سولز لايك", d: 2, q: "كم لاعب بالفريق الواحد في Elden Ring Nightreign؟", a: "ثلاثة" },
  { cat: "سولز لايك", d: 2, q: "وش يسمى الأبطال القابلين للعب في Nightreign؟", a: "Nightfarer", alt: ["نايتفيررز"] },
  { cat: "سولز لايك", d: 2, q: "وش كان منصب خازان في الإمبراطورية قبل ما يخونونه؟", a: "General", alt: ["جنرال"] },
  { cat: "سولز لايك", d: 3, q: "أول لعبة بسلسلة السولز، صدرت 2009 على PS3؟", a: "Demon's Souls", alt: ["ديمون سولز"] },
  { cat: "سولز لايك", d: 3, q: "الاستوديو اللي سوّى ريميك ديمون سولز 2020؟", a: "Bluepoint", alt: ["بلوبوينت"] },
  { cat: "سولز لايك", d: 3, q: "الاستوديو الكوري المطوّر لـ The First Berserker: Khazan؟", a: "Neople", alt: ["نيوبل"] },
  { cat: "سولز لايك", d: 3, q: "وش اسم منطقة/خريطة أحداث Nightreign؟", a: "Limveld", alt: ["ليمفيلد"] },
  { cat: "سولز لايك", d: 3, q: "وش اسم النايتفيرر الرامي بالقوس في Nightreign؟", a: "Ironeye", alt: ["آيروني"] },
  { cat: "سولز لايك", d: 3, q: "وش اسم إضافة Lies of P اللي صدرت 2025؟", a: "Overture" },
  { cat: "سولز لايك", d: 3, q: "وش الجائزة الكبرى اللي فازت فيها إلدن رينق سنة 2022؟", a: "لعبة العام" },
  // ===== أوفرواتش =====
  { cat: "أوفرواتش", d: 1, q: "وش اسم الغوريلا العالِم اللي جاء من القمر؟", a: "Winston", alt: ["وينستون"] },
  { cat: "أوفرواتش", d: 1, q: "البطلة السويسرية المعالجة صاحبة الأجنحة؟", a: "Mercy", alt: ["ميرسي"] },
  { cat: "أوفرواتش", d: 1, q: "البطلة البريطانية اللي ترجع بالزمن وتقول Cheers Love؟", a: "Tracer", alt: ["تريسر"] },
  { cat: "أوفرواتش", d: 1, q: "الشركة المطوّرة لأوفرواتش؟", a: "Blizzard", alt: ["بليزارد"] },
  { cat: "أوفرواتش", d: 2, q: "البطل الياباني اللي يطلق التنين من سهمه؟", a: "Hanzo", alt: ["هانزو"] },
  { cat: "أوفرواتش", d: 2, q: "أخو هانزو، النينجا السايبورغ؟", a: "Genji", alt: ["غينجي"] },
  { cat: "أوفرواتش", d: 2, q: "البطلة الكورية نجمة الستريمات اللي تقاتل داخل ميكا؟", a: "D.Va", alt: ["دي فا"] },
  { cat: "أوفرواتش", d: 2, q: "البطل السويدي القصير صانع التوريتات؟", a: "Torbjorn", alt: ["توربيورن"] },
  { cat: "أوفرواتش", d: 2, q: "كم لاعب بكل فريق في أوفرواتش 2؟", a: "خمسة" },
  { cat: "أوفرواتش", d: 3, q: "وش اسم المنظمة الشريرة في قصة أوفرواتش؟", a: "Talon", alt: ["تالون"] },
  { cat: "أوفرواتش", d: 3, q: "وش الاسم الحقيقي لسولجر 76؟", a: "Jack Morrison", alt: ["جاك موريسون"] },
  { cat: "أوفرواتش", d: 3, q: "البطل راينهارت من أي دولة؟", a: "ألمانيا" },
  { cat: "أوفرواتش", d: 3, q: "وش الاسم الحقيقي للريبر؟", a: "Gabriel Reyes", alt: ["غابرييل رييس"] },
  // ===== أزياء الشخصيات =====
  { cat: "أزياء الشخصيات", d: 1, q: "أفرول أزرق وقميص أحمر وقبعة حمراء عليها حرف M؟", a: "Mario", alt: ["ماريو"] },
  { cat: "أزياء الشخصيات", d: 1, q: "بدلة خضراء وقبعة خضراء طويلة وسيف وترس؟", a: "Link", alt: ["لينك"] },
  { cat: "أزياء الشخصيات", d: 1, q: "درع فضائي أخضر ثقيل وخوذة برتقالية العدسة ما تنشال أبد؟", a: "Master Chief", alt: ["ماستر شيف"] },
  { cat: "أزياء الشخصيات", d: 1, q: "قنفذ أزرق سريع بحذاء أحمر وقفازات بيضاء؟", a: "Sonic", alt: ["سونيك"] },
  { cat: "أزياء الشخصيات", d: 2, q: "محارب أصلع بجلد رمادي شاحب ووشم أحمر وفأسين بسلاسل؟", a: "Kratos", alt: ["كريتوس"] },
  { cat: "أزياء الشخصيات", d: 2, q: "قبعة كاوبوي ومعطف غربي وحزام مسدسات من لعبة عصابات الغرب؟", a: "Arthur Morgan", alt: ["آرثر مورغان"] },
  { cat: "أزياء الشخصيات", d: 2, q: "ضفيرة وشورت كاكي وتوب أزرق ومسدسين، مستكشفة قبور؟", a: "Lara Croft", alt: ["لارا كروفت"] },
  { cat: "أزياء الشخصيات", d: 2, q: "درع عسكري أخضر مصفّح وبندقية ضخمة، يذبح الشياطين بالمريخ؟", a: "Doom Slayer", alt: ["دووم سلاير"] },
  { cat: "أزياء الشخصيات", d: 3, q: "فستان أسود قصير وعصابة على العيون وشعر أبيض وسيف كاتانا؟", a: "2B" },
  { cat: "أزياء الشخصيات", d: 3, q: "فارس صغير بقناع أبيض بقرنين وعباءة سوداء وسيف مثل الإبرة؟", a: "Hollow Knight", alt: ["هولو نايت"] },
  { cat: "أزياء الشخصيات", d: 3, q: "معطف رمادي طويل وقبعة ثلاثية الأطراف ومنشار لحم، يصيد الوحوش بيارنام؟", a: "The Hunter", alt: ["الصياد"] },
  { cat: "أزياء الشخصيات", d: 3, q: "بدلة برتقالية متطورة ونظارة وعتلة حديد، عالم فيزياء صامت؟", a: "Gordon Freeman", alt: ["غوردون فريمان"] },
  // ===== ألعاب فيديو =====
  { cat: "ألعاب فيديو", d: 1, q: "الشركة المطوّرة للعبة Valorant؟", a: "Riot Games" },
  { cat: "ألعاب فيديو", d: 1, q: "في أي لعبة توجد خريطة Erangel؟", a: "PUBG", alt: ["ببجي"] },
  { cat: "ألعاب فيديو", d: 1, q: "وش اسم السباك الشهير بطل ألعاب نينتندو؟", a: "Mario", alt: ["ماريو"] },
  { cat: "ألعاب فيديو", d: 1, q: "لعبة الباتل رويال الشهيرة من Epic Games؟", a: "Fortnite", alt: ["فورتنايت"] },
  { cat: "ألعاب فيديو", d: 1, q: "وش اسم عملة لعبة فورتنايت؟", a: "V-Bucks" },
  { cat: "ألعاب فيديو", d: 2, q: "وش اسم بطل سلسلة God of War؟", a: "Kratos", alt: ["كريتوس"] },
  { cat: "ألعاب فيديو", d: 2, q: "الاستوديو المطوّر للعبة The Witcher 3؟", a: "CD Projekt Red" },
  { cat: "ألعاب فيديو", d: 2, q: "في أي سنة صدرت أول لعبة من سلسلة God of War؟", a: "2005" },
  { cat: "ألعاب فيديو", d: 2, q: "وش اسم خريطة الغبار الأشهر في Counter-Strike؟", a: "Dust 2" },
  { cat: "ألعاب فيديو", d: 2, q: "وش اسم بطلة سلسلة Tomb Raider؟", a: "Lara Croft", alt: ["لارا كروفت"] },
  { cat: "ألعاب فيديو", d: 3, q: "في أي سنة صدر أول جهاز بليستيشن؟", a: "1994" },
  { cat: "ألعاب فيديو", d: 3, q: "وش اللعبة الأكثر مبيعًا في التاريخ؟", a: "Minecraft", alt: ["ماينكرافت"] },
  { cat: "ألعاب فيديو", d: 3, q: "وش اسم المدينة اللي تدور فيها أحداث GTA V؟", a: "Los Santos", alt: ["لوس سانتوس"] },
  { cat: "ألعاب فيديو", d: 3, q: "في أي سنة صدرت النسخة الكاملة من Minecraft؟", a: "2011" },
  { cat: "ألعاب فيديو", d: 3, q: "وش اسم الفطر المسبب للعدوى في The Last of Us؟", a: "Cordyceps", alt: ["كورديسيبس"] },
  // ===== فيزياء =====
  { cat: "فيزياء", d: 1, q: "وش وحدة قياس القدرة الكهربائية؟", a: "واط" },
  { cat: "فيزياء", d: 1, q: "وش القوة اللي تسحب الأجسام نحو الأرض؟", a: "الجاذبية" },
  { cat: "فيزياء", d: 1, q: "كم درجة تجمد الماء بمقياس سيلسيوس؟", a: "صفر" },
  { cat: "فيزياء", d: 1, q: "وش مصدر الطاقة الرئيسي لكوكب الأرض؟", a: "الشمس" },
  { cat: "فيزياء", d: 1, q: "أيهما أسرع: الصوت أم الضوء؟", a: "الضوء" },
  { cat: "فيزياء", d: 2, q: "وش وحدة قياس التردد؟", a: "هيرتز" },
  { cat: "فيزياء", d: 2, q: "وش وحدة قياس الضغط في النظام الدولي؟", a: "باسكال" },
  { cat: "فيزياء", d: 2, q: "من العالم صاحب قوانين الحركة الثلاثة؟", a: "نيوتن" },
  { cat: "فيزياء", d: 2, q: "وش نوع العدسة اللي تجمّع أشعة الضوء بنقطة وحدة؟", a: "محدبة" },
  { cat: "فيزياء", d: 2, q: "وش وحدة قياس شدة التيار الكهربائي؟", a: "أمبير" },
  { cat: "فيزياء", d: 3, q: "وش معادلة أينشتاين الشهيرة اللي تربط الطاقة بالكتلة؟", a: "E=mc2" },
  { cat: "فيزياء", d: 3, q: "كم سرعة الصوت في الهواء تقريبًا بالمتر لكل ثانية؟", a: "343" },
  { cat: "فيزياء", d: 3, q: "القوة تساوي الكتلة ضرب التسارع — أي قانون من قوانين نيوتن؟", a: "الثاني" },
  { cat: "فيزياء", d: 3, q: "وش الجسيم عديم الشحنة داخل نواة الذرة؟", a: "النيوترون" },
  // ===== علوم =====
  { cat: "علوم", d: 1, q: "وش الكوكب المعروف بالكوكب الأحمر؟", a: "المريخ" },
  { cat: "علوم", d: 1, q: "وش الرمز الكيميائي للماء؟", a: "H2O" },
  { cat: "علوم", d: 1, q: "كم عدد كواكب المجموعة الشمسية؟", a: "ثمانية" },
  { cat: "علوم", d: 1, q: "وش الغاز اللي نتنفسه ونحتاجه للبقاء؟", a: "الأكسجين" },
  { cat: "علوم", d: 1, q: "وش أكبر عضو في جسم الإنسان؟", a: "الجلد" },
  { cat: "علوم", d: 2, q: "كم تبلغ سرعة الضوء تقريبًا بالكيلومتر في الثانية؟", a: "300 ألف" },
  { cat: "علوم", d: 2, q: "وش أصغر وحدة بنائية في الكائن الحي؟", a: "الخلية" },
  { cat: "علوم", d: 2, q: "وش العنصر الكيميائي اللي رمزه Fe؟", a: "الحديد" },
  { cat: "علوم", d: 2, q: "كم عدد أسنان الإنسان البالغ الكاملة؟", a: "32" },
  { cat: "علوم", d: 2, q: "وش أكبر كواكب المجموعة الشمسية؟", a: "المشتري" },
  { cat: "علوم", d: 3, q: "كم عدد عظام جسم الإنسان البالغ؟", a: "206" },
  { cat: "علوم", d: 3, q: "وش العنصر الأكثر وفرة في الكون؟", a: "الهيدروجين" },
  { cat: "علوم", d: 3, q: "كم درجة غليان الماء بمقياس فهرنهايت؟", a: "212" },
  { cat: "علوم", d: 3, q: "وش أقرب نجم إلى الأرض بعد الشمس؟", a: "بروكسيما سنتوري" },
  // ===== عام =====
  { cat: "عام", d: 1, q: "كم عدد أيام السنة الميلادية العادية؟", a: "365" },
  { cat: "عام", d: 1, q: "وش عاصمة المملكة العربية السعودية؟", a: "الرياض" },
  { cat: "عام", d: 1, q: "كم عدد ألوان قوس المطر؟", a: "سبعة" },
  { cat: "عام", d: 1, q: "وش الحيوان الملقّب بسفينة الصحراء؟", a: "الجمل" },
  { cat: "عام", d: 1, q: "كم عدد حروف اللغة العربية؟", a: "28" },
  { cat: "عام", d: 2, q: "وش أطول نهر في العالم بالتصنيف التقليدي؟", a: "النيل" },
  { cat: "عام", d: 2, q: "وش العملة الرسمية لليابان؟", a: "الين" },
  { cat: "عام", d: 2, q: "من المخترع الأشهر للمصباح الكهربائي التجاري؟", a: "إديسون" },
  { cat: "عام", d: 2, q: "وش أكبر محيطات العالم؟", a: "المحيط الهادئ" },
  { cat: "عام", d: 2, q: "كم عدد أضلاع الشكل السداسي؟", a: "ستة" },
  { cat: "عام", d: 3, q: "في أي سنة ميلادية أُعلن توحيد المملكة العربية السعودية؟", a: "1932" },
  { cat: "عام", d: 3, q: "وش أسرع طائر في العالم عند الانقضاض؟", a: "الشاهين" },
  { cat: "عام", d: 3, q: "كم عدد اللغات الرسمية المعتمدة في الأمم المتحدة؟", a: "ست لغات" },
  { cat: "عام", d: 3, q: "كم عدد مفاتيح البيانو القياسي؟", a: "88" },
  { cat: "عام", d: 3, q: "وش أصغر دولة في العالم مساحةً؟", a: "الفاتيكان" },
  // ===== رياضة =====
  { cat: "رياضة", d: 1, q: "كم لاعبًا في فريق كرة القدم داخل الملعب؟", a: "11" },
  { cat: "رياضة", d: 1, q: "أي نادٍ سعودي يُلقّب بالزعيم؟", a: "الهلال" },
  { cat: "رياضة", d: 1, q: "كم دقيقة مدة الشوط الواحد في كرة القدم؟", a: "45" },
  { cat: "رياضة", d: 1, q: "في أي دولة أُقيم كأس العالم 2022؟", a: "قطر" },
  { cat: "رياضة", d: 1, q: "من اللاعب الأرجنتيني الملقّب بالبرغوث؟", a: "ميسي" },
  { cat: "رياضة", d: 2, q: "كم مرة فازت البرازيل بكأس العالم؟", a: "خمس مرات" },
  { cat: "رياضة", d: 2, q: "من كان هدّاف كأس العالم 2022؟", a: "مبابي" },
  { cat: "رياضة", d: 2, q: "كم عدد لاعبي الكرة الطائرة داخل الملعب؟", a: "6" },
  { cat: "رياضة", d: 2, q: "وش الدولة الفائزة بأول كأس عالم سنة 1930؟", a: "الأوروغواي" },
  { cat: "رياضة", d: 2, q: "من اللاعب البرازيلي الملقّب بالظاهرة؟", a: "رونالدو" },
  { cat: "رياضة", d: 3, q: "في أي مدينة أُقيمت أولمبياد 2016؟", a: "ريو دي جانيرو" },
  { cat: "رياضة", d: 3, q: "من الملاكم الملقّب بالأعظم؟", a: "محمد علي كلاي" },
  { cat: "رياضة", d: 3, q: "كم يبلغ طول سباق الماراثون بالكيلومترات تقريبًا؟", a: "42" },
  { cat: "رياضة", d: 3, q: "من حارس المرمى الوحيد الفائز بالكرة الذهبية؟", a: "ياشين" },
  { cat: "رياضة", d: 3, q: "كم مجموعة كحد أقصى في مباريات التنس الكبرى للرجال؟", a: "خمس" },
  // ===== أفلام ومسلسلات =====
  { cat: "أفلام ومسلسلات", d: 1, q: "وش اسم مدرسة السحر في سلسلة هاري بوتر؟", a: "هوغوورتس" },
  { cat: "أفلام ومسلسلات", d: 1, q: "من أدّى دور الجوكر في فيلم Joker سنة 2019؟", a: "خواكين فينيكس" },
  { cat: "أفلام ومسلسلات", d: 1, q: "وش اسم الأسد الصغير بطل فيلم ديزني الشهير؟", a: "سيمبا" },
  { cat: "أفلام ومسلسلات", d: 1, q: "وش اسم السفينة في فيلم الغرق الشهير سنة 1997؟", a: "تايتانيك" },
  { cat: "أفلام ومسلسلات", d: 1, q: "من بطل سلسلة أفلام Mission Impossible؟", a: "توم كروز" },
  { cat: "أفلام ومسلسلات", d: 2, q: "وش اسم عائلة التنانين في Game of Thrones؟", a: "تارغيريان" },
  { cat: "أفلام ومسلسلات", d: 2, q: "من مخرج فيلمي Inception و Interstellar؟", a: "كريستوفر نولان" },
  { cat: "أفلام ومسلسلات", d: 2, q: "وش المسلسل الإسباني الشهير عن سرقة دار سك العملة؟", a: "La Casa de Papel" },
  { cat: "أفلام ومسلسلات", d: 2, q: "وش اسم الكوكب في فيلم Avatar؟", a: "باندورا" },
  { cat: "أفلام ومسلسلات", d: 2, q: "من الممثل اللي أدّى شخصية القبطان جاك سبارو؟", a: "جوني ديب" },
  { cat: "أفلام ومسلسلات", d: 3, q: "في أي موسم من True Detective ظهرت شخصية راست كول؟", a: "الموسم الأول" },
  { cat: "أفلام ومسلسلات", d: 3, q: "وش أول فيلم غير إنجليزي يفوز بأوسكار أفضل فيلم؟", a: "Parasite" },
  { cat: "أفلام ومسلسلات", d: 3, q: "وش اسم شركة الطيران اللي تحطمت طائرتها في مسلسل LOST؟", a: "أوشيانك" },
  { cat: "أفلام ومسلسلات", d: 3, q: "من مخرج فيلم The Seventh Seal؟", a: "بيرغمان" },
  { cat: "أفلام ومسلسلات", d: 3, q: "في أي مدينة أمريكية تدور أحداث مسلسل The Wire؟", a: "بالتيمور" },
  // ===== تاريخ =====
  { cat: "تاريخ", d: 1, q: "في أي مدينة وُلد النبي محمد ﷺ؟", a: "مكة" },
  { cat: "تاريخ", d: 1, q: "من أول الخلفاء الراشدين؟", a: "أبو بكر الصديق" },
  { cat: "تاريخ", d: 1, q: "من الملك مؤسس المملكة العربية السعودية الحديثة؟", a: "عبدالعزيز" },
  { cat: "تاريخ", d: 1, q: "في أي دولة توجد أهرامات الجيزة؟", a: "مصر" },
  { cat: "تاريخ", d: 1, q: "في أي سنة انتهت الحرب العالمية الثانية؟", a: "1945" },
  { cat: "تاريخ", d: 2, q: "من القائد اللي فتح الأندلس؟", a: "طارق بن زياد" },
  { cat: "تاريخ", d: 2, q: "في أي سنة بدأت الحرب العالمية الأولى؟", a: "1914" },
  { cat: "تاريخ", d: 2, q: "من القائد اللي استعاد القدس بعد معركة حطين؟", a: "صلاح الدين" },
  { cat: "تاريخ", d: 2, q: "وش الحضارة اللي بنت مدينة البتراء؟", a: "الأنباط" },
  { cat: "تاريخ", d: 2, q: "من القائد المغولي مؤسس إمبراطورية المغول؟", a: "جنكيز خان" },
  { cat: "تاريخ", d: 3, q: "في أي سنة أُلغيت السلطنة العثمانية رسميًا؟", a: "1922" },
  { cat: "تاريخ", d: 3, q: "وش المعركة اللي انتصر فيها المسلمون على المغول؟", a: "عين جالوت" },
  { cat: "تاريخ", d: 3, q: "في أي سنة سقطت غرناطة آخر معاقل الأندلس؟", a: "1492" },
  { cat: "تاريخ", d: 3, q: "من القائد القرطاجي اللي عبر جبال الألب بالفيلة؟", a: "حنبعل" },
  { cat: "تاريخ", d: 3, q: "في أي سنة سقط جدار برلين؟", a: "1989" },
  // ===== جغرافيا =====
  { cat: "جغرافيا", d: 1, q: "وش أكبر قارات العالم مساحةً؟", a: "آسيا" },
  { cat: "جغرافيا", d: 1, q: "وش عاصمة اليابان؟", a: "طوكيو" },
  { cat: "جغرافيا", d: 1, q: "وش أكبر صحراء حارة في العالم؟", a: "الصحراء الكبرى" },
  { cat: "جغرافيا", d: 1, q: "وش البحر الواقع غرب السعودية؟", a: "البحر الأحمر" },
  { cat: "جغرافيا", d: 1, q: "وش عاصمة فرنسا؟", a: "باريس" },
  { cat: "جغرافيا", d: 2, q: "وش أكبر دولة عربية من حيث المساحة؟", a: "الجزائر" },
  { cat: "جغرافيا", d: 2, q: "وش أطول سلسلة جبال في العالم؟", a: "الأنديز" },
  { cat: "جغرافيا", d: 2, q: "وش أكبر دولة في العالم مساحةً؟", a: "روسيا" },
  { cat: "جغرافيا", d: 2, q: "وش عاصمة أستراليا؟", a: "كانبرا" },
  { cat: "جغرافيا", d: 2, q: "وش المضيق الفاصل بين آسيا وأفريقيا جنوب البحر الأحمر؟", a: "باب المندب" },
  { cat: "جغرافيا", d: 3, q: "وش أعمق نقطة في محيطات العالم؟", a: "خندق ماريانا" },
  { cat: "جغرافيا", d: 3, q: "وش الدولة اللي تضم أكبر عدد جزر في العالم؟", a: "السويد" },
  { cat: "جغرافيا", d: 3, q: "وش أعلى شلال في العالم؟", a: "شلال أنجل" },
  { cat: "جغرافيا", d: 3, q: "وش أكبر بحيرة في العالم؟", a: "بحر قزوين" },
  { cat: "جغرافيا", d: 3, q: "كم دولة تشترك بالحدود البرية مع الصين؟", a: "14" },
  // ===== تقنية =====
  { cat: "تقنية", d: 1, q: "من أسّس شركة آبل مع ستيف ووزنياك؟", a: "ستيف جوبز" },
  { cat: "تقنية", d: 1, q: "وش يعني اختصار AI بالعربي؟", a: "الذكاء الاصطناعي" },
  { cat: "تقنية", d: 1, q: "وش الشركة المالكة لواتساب وإنستقرام؟", a: "ميتا" },
  { cat: "تقنية", d: 1, q: "وش محرك البحث الأشهر في العالم؟", a: "قوقل" },
  { cat: "تقنية", d: 1, q: "من الرئيس التنفيذي لشركتي تسلا وسبيس إكس؟", a: "إيلون ماسك" },
  { cat: "تقنية", d: 2, q: "في أي سنة صدر أول آيفون؟", a: "2007" },
  { cat: "تقنية", d: 2, q: "وش الشركة اللي تطوّر نظام أندرويد؟", a: "قوقل" },
  { cat: "تقنية", d: 2, q: "وش يعني اختصار CPU بالعربي؟", a: "وحدة المعالجة المركزية" },
  { cat: "تقنية", d: 2, q: "وش اسم المساعد الصوتي من آبل؟", a: "سيري" },
  { cat: "تقنية", d: 2, q: "وش الشركة المطوّرة لنظام ويندوز؟", a: "مايكروسوفت" },
  { cat: "تقنية", d: 3, q: "في أي سنة أُطلق موقع يوتيوب؟", a: "2005" },
  { cat: "تقنية", d: 3, q: "من مؤسس شركة أمازون؟", a: "جيف بيزوس" },
  { cat: "تقنية", d: 3, q: "وش الاسم الأول لمحرك بحث قوقل قبل تغييره؟", a: "BackRub" },
  { cat: "تقنية", d: 3, q: "وش اسم أول قمر صناعي أُطلق إلى الفضاء؟", a: "سبوتنيك" },
  { cat: "تقنية", d: 3, q: "وش اسم الشبكة الأمريكية اللي سبقت الإنترنت؟", a: "أربانت" },

  // ============ مين البوس؟ (من السياق مو من الاسم) ============
  { cat: "مين البوس؟", d: 1, q: "أول ما تطلع للعالم المفتوح بإلدن رينق، فارس ذهبي على حصان يذبحك بأول خمس دقايق — مين؟", a: "Tree Sentinel", alt: ["حارس الشجرة","تري سنتينل"] },
  { cat: "مين البوس؟", d: 1, q: "أول جدار حقيقي يوقفك بإلدن رينق، يقفز بالهوا ويرمي مطرقة ضوء ويقول لك طموحك أحمق — مين؟", a: "Margit", alt: ["مارغيت"] },
  { cat: "مين البوس؟", d: 1, q: "أول بوس بدارك سولز 3، يقاتلك عادي ثم تنغرز فيه أفعى سوداء ويتحول بالمرحلة الثانية — مين؟", a: "Iudex Gundyr", alt: ["غوندر","Gundyr"] },
  { cat: "مين البوس؟", d: 1, q: "أول بوس بـ Lies of P، دمية ضخمة تقاتلك وسط ساحة المهرجان المدمّرة — مين؟", a: "Parade Master", alt: ["سيد الاستعراض"] },
  { cat: "مين البوس؟", d: 2, q: "هذا القناع الأول لبوس تقاتله مرتين — بالبداية بقناع، وبالعاصمة يكشف اسمه الملكي الحقيقي. وش اسمه الحقيقي؟", a: "Morgott", alt: ["مورغوت"] },
  { cat: "مين البوس؟", d: 2, q: "بوس بلودبورن اللي يشتغل قبله صندوق موسيقى يوقّفه لحظة — مين؟", a: "Father Gascoigne", alt: ["غاسكوين","الأب غاسكوين"] },
  { cat: "مين البوس؟", d: 2, q: "بوس سيكيرو اللي يقطع راسه ويكمل يقاتلك بالمرحلة الثانية وهو بلا راس — مين؟", a: "Guardian Ape", alt: ["القرد الحارس"] },
  { cat: "مين البوس؟", d: 2, q: "بوسة إلدن رينق اللي كل ما ضربتك ترجّع لنفسها صحة، وتنتظرك بأعماق شجرة Haligtree — مين؟", a: "Malenia", alt: ["ماليينيا"] },
  { cat: "مين البوس؟", d: 2, q: "بوس دارك سولز الأول: ذئب أبيض عملاق يمسك سيف ضخم بفمه ويحمي قبر سيده — مين؟", a: "Sif", alt: ["سيف الذئب"] },
  { cat: "مين البوس؟", d: 3, q: "البوس اللي تقاتله مباشرة قبل Elden Beast، ويجمع بين المطرقة والسحر لأنه نصف الإلهة الثاني — مين؟", a: "Radagon", alt: ["رادغون"] },
  { cat: "مين البوس؟", d: 3, q: "بوس إلدن رينق اللي تقاتله وأنت راكب حصانك بمهرجان في Caelid، وبالمرحلة الثانية يطير للسما ويرتطم بك كنيزك — مين؟", a: "Radahn", alt: ["رادان"] },
  { cat: "مين البوس؟", d: 3, q: "بوس Lies of P: تقاتل دمية ضخمة، وبعد ما تكسرها يطلع من داخلها صديق البطل — مين اللي يطلع؟", a: "Romeo", alt: ["روميو"] },
  { cat: "مين البوس؟", d: 3, q: "بوس دارك سولز الأول اللي معركته موسيقاها بيانو حزين لأنه ملك انطفأ نوره — مين؟", a: "Gwyn", alt: ["غوين"] },
  { cat: "مين البوس؟", d: 3, q: "بوس بلودبورن الأخير: عجوز على كرسي متحرك تحت شجرة، كان معلّم الصيادين قبل لا يصير خصمك — مين؟", a: "Gehrman", alt: ["غيرمان"] },
  // ============ وين المكان؟ ============
  { cat: "وين المكان؟", d: 1, q: "منطقة إلدن رينق الأولى: عشب أخضر وأطلال وشجرة ذهبية عملاقة بالأفق، ومنها يبدأ كل لاعب — وين؟", a: "Limgrave", alt: ["ليمغريف"] },
  { cat: "وين المكان؟", d: 1, q: "مدينة الوحوش الفيكتورية: شوارع ضيقة وفوانيس وذئاب بشرية، والأهالي مغلّقين بيوتهم من الرعب — وين؟", a: "Yharnam", alt: ["يارنام"] },
  { cat: "وين المكان؟", d: 2, q: "أرض حمراء كلها تعفّن وذباب عملاق، سمّاها اللاعبين جهنم إلدن رينق — وين؟", a: "Caelid", alt: ["كايليد"] },
  { cat: "وين المكان؟", d: 2, q: "مدينة الآلهة بدارك سولز: كل شي ذهبي وضخم، وحراسها فارس رمح وعملاق بمطرقة — وين؟", a: "Anor Londo", alt: ["أنور لوندو"] },
  { cat: "وين المكان؟", d: 2, q: "قصر على جبل بركاني بإلدن رينق، صاحبه انقلب على الآلهة وصار ثعبانًا عملاقًا — وين؟", a: "Volcano Manor", alt: ["قصر البركان"] },
  { cat: "وين المكان؟", d: 3, q: "تحت الأرض بإلدن رينق: سماء مزيفة فيها نجوم، وأنهار وأشجار مضيئة — وين؟", a: "Siofra River", alt: ["نهر سيوفرا","سيوفرا"] },
  { cat: "وين المكان؟", d: 3, q: "المنطقة اللي تنفتح لك بإلدن رينق مباشرة بعد ما تسقط عملاق النار — وين؟", a: "Farum Azula", alt: ["فاروم أزولا"] },
  { cat: "وين المكان؟", d: 3, q: "مدينة تحت الأرض بإلدن رينق فيها سيوف عملاقة مغروزة، وهي موطن رانّي المفقود — وين؟", a: "Nokron", alt: ["نوكرون"] },
  // ============ مين قالها؟ ============
  { cat: "مين قالها؟", d: 1, q: "مين صاحب عبارة «الشتاء قادم» شعار عائلته؟", a: "Stark", alt: ["ستارك","عائلة ستارك"] },
  { cat: "مين قالها؟", d: 1, q: "بأوفرواتش، مين البطلة اللي تقول «الفرسان وصلوا» وهي تدخل بسرعة البرق؟", a: "Tracer", alt: ["تريسر"] },
  { cat: "مين قالها؟", d: 2, q: "بإلدن رينق، مين اللي تعرّف عن نفسها بأنها نصل ميكيلا وما عرفت الهزيمة أبدًا؟", a: "Malenia", alt: ["ماليينيا"] },
  { cat: "مين قالها؟", d: 2, q: "بدارك سولز، مين الفارس المبتسم اللي جملته صارت شعار المجتمع كله عن الشمس؟", a: "Solaire", alt: ["سولير"] },
  { cat: "مين قالها؟", d: 2, q: "بمسلسل True Detective، مين قال إن الزمن دائرة مسطحة؟", a: "Rust Cohle", alt: ["راست كول","راست"] },
  { cat: "مين قالها؟", d: 3, q: "بمسلسل Breaking Bad، مين قال «أنا اللي أطرق الباب»؟", a: "Walter White", alt: ["والتر وايت","هايزنبرغ"] },
  { cat: "مين قالها؟", d: 3, q: "ببلودبورن، مين العجوز اللي حذّر من الدم القديم وقال ارتعبوا منه؟", a: "Willem", alt: ["ويليم","المعلم ويليم"] },
  // ============ وش الغرض؟ ============
  { cat: "وش الغرض؟", d: 1, q: "شيء تجلس عنده فترتاح، تنشحن قواريرك، بس كل الأعداء ترجع تعيش — وش هو؟", a: "Bonfire", alt: ["بونفاير","نار المخيم","Site of Grace","موقع النعمة"] },
  { cat: "وش الغرض؟", d: 1, q: "قارورتين بإلدن رينق: وحدة حمراء تعيد الصحة ووحدة زرقاء تعيد المانا — وش اسمهما؟", a: "Flask", alt: ["الفلاسك","فلاسك"] },
  { cat: "وش الغرض؟", d: 2, q: "غرض ببلودبورن يوقف بوس معيّن لحظات لأنه يذكّره بعائلته — وش هو؟", a: "Music Box", alt: ["صندوق الموسيقى","علبة الموسيقى"] },
  { cat: "وش الغرض؟", d: 2, q: "غرض بإلدن رينق تحصله من ساحرة بأطلال، وبدونه ما تقدر تستدعي أرواح تقاتل معك — وش هو؟", a: "Spirit Calling Bell", alt: ["جرس استدعاء الأرواح","الجرس"] },
  { cat: "وش الغرض؟", d: 2, q: "سلاح ببلودبورن يتحول من منشار قصير لساطور طويل بضغطة زر — وش هو؟", a: "Saw Cleaver", alt: ["منشار الصياد","الساطور"] },
  { cat: "وش الغرض؟", d: 3, q: "أداة سيكيرو المثبتة بذراع البطل، تركّب عليها خطاف ومظلة وقاذف نار — وش اسمها؟", a: "Shinobi Prosthetic", alt: ["الذراع الصناعية","الذراع"] },
  { cat: "وش الغرض؟", d: 3, q: "ميزة Lies of P اللي تركّب فيها مقبض سلاح على نصل سلاح ثاني وتطلع سلاح جديد — وش اسمها؟", a: "Weapon Assembly", alt: ["دمج الأسلحة","تركيب الأسلحة"] },
  // ============ الرابط المشترك ============
  { cat: "الرابط المشترك", d: 1, q: "Sekiro و Bloodborne و Elden Ring — وش الاستوديو اللي يجمعهم؟", a: "FromSoftware", alt: ["فروم سوفتوير","فروم"] },
  { cat: "الرابط المشترك", d: 1, q: "Mario و Link و Samus — وش الشركة اللي تجمعهم؟", a: "Nintendo", alt: ["نينتندو"] },
  { cat: "الرابط المشترك", d: 1, q: "Erangel و Miramar و Sanhok — وش اللعبة اللي تجمعهم؟", a: "PUBG", alt: ["ببجي"] },
  { cat: "الرابط المشترك", d: 2, q: "Dust 2 و Mirage و Inferno — وش اللعبة اللي تجمعهم؟", a: "Counter-Strike", alt: ["كاونتر سترايك","CS"] },
  { cat: "الرابط المشترك", d: 2, q: "Kratos و Atreus و Freya — وش السلسلة اللي تجمعهم؟", a: "God of War", alt: ["جود أوف وور"] },
  { cat: "الرابط المشترك", d: 2, q: "The Wire و True Detective و Barry — وش الشبكة اللي تجمعهم؟", a: "HBO", alt: ["إتش بي أو"] },
  { cat: "الرابط المشترك", d: 3, q: "Winston و Hammond و Roadhog — وش القاسم المشترك بين الثلاثة بأوفرواتش؟", a: "حيوانات", alt: ["animals","كلهم حيوانات"] },
  { cat: "الرابط المشترك", d: 3, q: "الهيدروجين والهيليوم والليثيوم — وش يجمعهم بترتيب الجدول الدوري؟", a: "أول ثلاثة عناصر", alt: ["الأخف","أخف العناصر"] },
  // ============ قبل ولا بعد؟ ============
  { cat: "قبل ولا بعد؟", d: 1, q: "أيهما صدر أول: Bloodborne ولا Dark Souls 3؟", a: "Bloodborne", alt: ["بلودبورن"] },
  { cat: "قبل ولا بعد؟", d: 1, q: "أيهما صدر أول: Sekiro ولا Elden Ring؟", a: "Sekiro", alt: ["سيكيرو"] },
  { cat: "قبل ولا بعد؟", d: 2, q: "أيهما أقدم: Demon's Souls ولا Dark Souls؟", a: "Demon's Souls", alt: ["ديمون سولز"] },
  { cat: "قبل ولا بعد؟", d: 2, q: "بـ Lies of P: مين تقابله أول، Parade Master ولا King of Puppets؟", a: "Parade Master", alt: ["سيد الاستعراض"] },
  { cat: "قبل ولا بعد؟", d: 2, q: "أيهما أطلق أول: يوتيوب ولا فيسبوك؟", a: "فيسبوك", alt: ["Facebook"] },
  { cat: "قبل ولا بعد؟", d: 3, q: "أيهما وقع أول: سقوط جدار برلين ولا تفكك الاتحاد السوفيتي؟", a: "جدار برلين", alt: ["سقوط جدار برلين"] },
  { cat: "قبل ولا بعد؟", d: 3, q: "أيهما صدر أول: أول آيفون ولا أول أندرويد تجاري؟", a: "الآيفون", alt: ["iPhone","آيفون"] },
  { cat: "قبل ولا بعد؟", d: 3, q: "بإلدن رينق: مين تقاتل مباشرة قبل Farum Azula تنفتح؟", a: "Fire Giant", alt: ["عملاق النار"] },
  // ============ إيموجي ============
  { cat: "إيموجي", d: 1, q: "خمّن اللعبة: 🍄👨‍🔧🏰", a: "Mario", alt: ["ماريو","سوبر ماريو"] },
  { cat: "إيموجي", d: 1, q: "خمّن اللعبة: 🥷🦾🇯🇵", a: "Sekiro", alt: ["سيكيرو"] },
  { cat: "إيموجي", d: 1, q: "خمّن الفيلم: ⚡🧙‍♂️🏫", a: "Harry Potter", alt: ["هاري بوتر"] },
  { cat: "إيموجي", d: 2, q: "خمّن اللعبة: 🐺🌙🩸🏙️", a: "Bloodborne", alt: ["بلودبورن"] },
  { cat: "إيموجي", d: 2, q: "خمّن اللعبة: 🗡️🌳👑💍", a: "Elden Ring", alt: ["إلدن رينق"] },
  { cat: "إيموجي", d: 2, q: "خمّن الفيلم: 🚢🧊💔", a: "Titanic", alt: ["تايتانيك"] },
  { cat: "إيموجي", d: 2, q: "خمّن اللعبة: ⛏️🟩🐷", a: "Minecraft", alt: ["ماينكرافت"] },
  { cat: "إيموجي", d: 3, q: "خمّن اللعبة: 🍄🧟👧🎸", a: "The Last of Us", alt: ["لاست أوف أص","ذا لاست أوف أس"] },
  { cat: "إيموجي", d: 3, q: "خمّن اللعبة: 🤠🐴🔫🏜️", a: "Red Dead Redemption", alt: ["ريد ديد"] },
  { cat: "إيموجي", d: 3, q: "خمّن المسلسل: 🐉❄️👑⚔️", a: "Game of Thrones", alt: ["قيم أوف ثرونز","صراع العروش"] },
  // ============ دليلين ============
  { cat: "دليلين", d: 1, q: "دليل ١: أصلع وبشرة رمادية. دليل ٢: ذبح آلهة اليونان ثم انتقل للشمال. مين؟", a: "Kratos", alt: ["كريتوس"] },
  { cat: "دليلين", d: 1, q: "دليل ١: كوري الصنع. دليل ٢: بطلها دمية مستوحاة من بينوكيو. وش اللعبة؟", a: "Lies of P", alt: ["لايز أوف بي"] },
  { cat: "دليلين", d: 2, q: "دليل ١: استوديو ياباني. دليل ٢: ما يعطيك خريطة أهداف ولا مؤشرات، يخليك تتوه. مين؟", a: "FromSoftware", alt: ["فروم سوفتوير"] },
  { cat: "دليلين", d: 2, q: "دليل ١: 206 عظمة. دليل ٢: 46 كروموسوم. عن مين نتكلم؟", a: "الإنسان", alt: ["human","البشر"] },
  { cat: "دليلين", d: 2, q: "دليل ١: بطل صامت ما ينطق ولا كلمة. دليل ٢: سلاحه الأشهر عتلة حديد. مين؟", a: "Gordon Freeman", alt: ["غوردون فريمان"] },
  { cat: "دليلين", d: 3, q: "دليل ١: أعمق نقطة بالمحيطات. دليل ٢: بالمحيط الهادئ قرب اليابان. وش هي؟", a: "خندق ماريانا", alt: ["Mariana Trench","ماريانا"] },
  { cat: "دليلين", d: 3, q: "دليل ١: ملحّنة يابانية. دليل ٢: موسيقى بوسات دارك سولز وإلدن رينق. مين؟", a: "Yuka Kitamura", alt: ["يوكا كيتامورا"] },
  { cat: "دليلين", d: 3, q: "دليل ١: عاصمة ما فيها أشهر مدن بلدها. دليل ٢: البلد قارة كاملة. وش العاصمة؟", a: "كانبرا", alt: ["Canberra"] },
  // ============ الأغرب ============
  { cat: "الأغرب", d: 1, q: "مين الغريب: المريخ، الزهرة، القمر، عطارد؟", a: "القمر", alt: ["Moon","لأنه قمر مو كوكب"] },
  { cat: "الأغرب", d: 1, q: "مين الغريب: الهلال، النصر، الاتحاد، برشلونة؟", a: "برشلونة", alt: ["Barcelona"] },
  { cat: "الأغرب", d: 2, q: "مين الغريب: Sekiro، Bloodborne، Nioh، Elden Ring؟", a: "Nioh", alt: ["نيوه"] },
  { cat: "الأغرب", d: 2, q: "مين الغريب: Valorant، Counter-Strike، Overwatch، Minecraft؟", a: "Minecraft", alt: ["ماينكرافت"] },
  { cat: "الأغرب", d: 2, q: "مين الغريب: حديد، ذهب، أكسجين، نحاس؟", a: "الأكسجين", alt: ["Oxygen","أكسجين"] },
  { cat: "الأغرب", d: 3, q: "مين الغريب: طوكيو، باريس، سيدني، الرياض؟", a: "سيدني", alt: ["Sydney"] },
  { cat: "الأغرب", d: 3, q: "مين الغريب: Genji، Hanzo، Mercy، D.Va — من ناحية الجنسية؟", a: "Mercy", alt: ["ميرسي"] },
  { cat: "الأغرب", d: 3, q: "مين الغريب: نيوتن، أينشتاين، هايزنبرغ، داروين؟", a: "داروين", alt: ["Darwin"] },
  // ============ لو كنت مكانك ============
  { cat: "لو كنت مكانك", d: 1, q: "طاح منك 50 ألف رون بإلدن رينق، ورحت تجيبها ومت مرة ثانية بالطريق — وش صار بالرونات الأولى؟", a: "راحت", alt: ["ضاعت","انمسحت","تروح","خسرتها"] },
  { cat: "لو كنت مكانك", d: 1, q: "ببلودبورن انضربت وطاحت صحتك — كيف ترجّع جزء منها بدون ما تشرب علاج؟", a: "تضرب العدو", alt: ["Rally","بالهجوم المرتد","ترد الضربة"] },
  { cat: "لو كنت مكانك", d: 2, q: "بسيكيرو مت وقمت بالإحياء مرات كثيرة — وش المرض اللي ينتشر بين شخصيات القصة بسببك؟", a: "Dragonrot", alt: ["تعفن التنين","دراقون روت"] },
  { cat: "لو كنت مكانك", d: 2, q: "بإلدن رينق تبي تستدعي صديقك يساعدك بالبوس — وش لازم تسوي أول قبل الاستدعاء؟", a: "تستخدم Furlcalling Finger Remedy", alt: ["دواء الأصابع","تشغّل علامات الاستدعاء","Furlcalling"] },
  { cat: "لو كنت مكانك", d: 2, q: "بدارك سولز وصلت لبوس صعب ومعك أرواح بشرية — وش الفايدة من استخدامها قبل المعركة؟", a: "ترجع بشري", alt: ["تصير Human","تستدعي مساعدة","Humanity"] },
  { cat: "لو كنت مكانك", d: 3, q: "بسيكيرو تواجه عدو مدرّع ضربك ما يأثر فيه — وش الأسلوب الصح بدل ما تضربه؟", a: "تكسر توازنه", alt: ["Posture","البوستشر","الصد المثالي","Deflect"] },
  { cat: "لو كنت مكانك", d: 3, q: "بإلدن رينق سلاحك +9 وتبي توصله للحد الأقصى — وش الحجر اللي تحتاجه؟", a: "Smithing Stone", alt: ["حجر الحدادة","سمثنق ستون"] },
  // ===== إضافات: بوسات السولز =====
  { cat: "بوسات السولز", d: 2, q: "بوس إلدن رينق الفارس الملطخ بالدم اللي يحرس قصر ليرنيا؟", a: "Loretta", alt: ["لوني"] },
  { cat: "بوسات السولز", d: 2, q: "بوس دارك سولز الأول التنين اللي تقدر تقطع ذيله وتاخذ سيفه؟", a: "Bridge Dragon", alt: ["تنين الجسر"] },
  { cat: "بوسات السولز", d: 2, q: "بوس بلودبورن الأول اللي يواجهك بساحة يارنام المركزية؟", a: "Cleric Beast" },
  { cat: "بوسات السولز", d: 2, q: "بوس سيكيرو المحارب الأزرق أول قتال حقيقي عند البرج؟", a: "جينيتشيرو" },
  { cat: "بوسات السولز", d: 2, q: "بوس Lies of P الغاضب المتحول من عامل، ثاني بوس رئيسي؟", a: "King of Puppets", alt: ["الملك المجنون"] },
  { cat: "بوسات السولز", d: 2, q: "بوس إلدن رينق التنين العملاق في بحيرة ليرنيا؟", a: "Agheel", alt: ["أغيل"] },
  { cat: "بوسات السولز", d: 3, q: "بوس إلدن رينق الجنرال الأحمر اللي كان يحمل النجوم على كتفه؟", a: "Radahn", alt: ["رادان"] },
  { cat: "بوسات السولز", d: 3, q: "بوسة إلدن رينق الجميلة المتوحشة زوجة رادغون السابقة؟", a: "Rennala", alt: ["رينالا"] },
  { cat: "بوسات السولز", d: 3, q: "بوس دارك سولز 3 السيف الرمادي حامي مقبرة العمالقة؟", a: "Abyss Watchers", alt: ["أبيس ووتشرز"] },
  { cat: "بوسات السولز", d: 3, q: "بوس بلودبورن الرئيسي اللي يمثل الدم القديم بكنيسة هيلث؟", a: "Ebrietas" },
  { cat: "بوسات السولز", d: 3, q: "بوس دارك سولز الأول التوأم اللي بأنور لوندو؟", a: "Ornstein and Smough", alt: ["أورنشتاين وسموغ"] },
  { cat: "بوسات السولز", d: 3, q: "بوس سيكيرو الراهب الخالد اللي ما يموت إلا بالساموراي المقدس؟", a: "Guardian Ape", alt: ["الراهب الخالد"] },
  { cat: "بوسات السولز", d: 3, q: "بوس Nightreign التنين الجليدي الطائر بين الليل والنهار؟", a: "Adel", alt: ["أدل"] },
  { cat: "بوسات السولز", d: 3, q: "بوس Lies of P الفتاة الشبح اللي تقاتل بجسد أختها؟", a: "Laxasia", alt: ["لاكسيا"] },
  { cat: "بوسات السولز", d: 3, q: "بوس دارك سولز 3 النهائي، تجسيد سادة الرماد كلهم؟", a: "Soul of Cinder", alt: ["روح سيندر"] },
  { cat: "بوسات السولز", d: 3, q: "بوس Khazan الأول اللي يواجهك بالجليد ويقتلك بضربتين؟", a: "Yurikhan", alt: ["يوريكس"] },
  // ===== إضافات: لور السولز =====
  { cat: "لور السولز", d: 2, q: "وش اسم منطقة إلدن رينق المسمومة تحت شجرة الإرد الحمراء؟", a: "بحيرة القيح" },
  { cat: "لور السولز", d: 2, q: "من الساحرة الزرقاء بنت رينالا اللي تعرض عليك نهاية النجوم؟", a: "Ranni", alt: ["رانّي"] },
  { cat: "لور السولز", d: 2, q: "وش اسم قبيلة الأقزام في Lies of P اللي تبيعك العتاد؟", a: "Alchemist", alt: ["الألكيميست"] },
  { cat: "لور السولز", d: 2, q: "وش تسمى الأرواح اللي تستدعيها بإلدن رينق للمساعدة بالقتال؟", a: "Spirit Ashes", alt: ["الأرواح الرمادية"] },
  { cat: "لور السولز", d: 3, q: "من صانع الدمى الأب الروحي لبطل Lies of P؟", a: "Geppetto", alt: ["جيبيتو"] },
  { cat: "لور السولز", d: 3, q: "وش المرض اللي أصاب أهل كرات وحوّلهم في Lies of P؟", a: "Petrification", alt: ["بيتروفكشن"] },
  { cat: "لور السولز", d: 3, q: "من الإله الأول اللي أشعل النار الأولى بدارك سولز؟", a: "Gwyn", alt: ["غوين"] },
  { cat: "لور السولز", d: 3, q: "وش اسم الديدان الشيطانية اللي تصيب سكان يارنام؟", a: "Scourge Beast", alt: ["الوباء"] },
  { cat: "لور السولز", d: 3, q: "وش القانون الأربعة اللي يقيّده ميكيلا ليصير إلهًا بـ DLC إلدن رينق؟", a: "Abandonment", alt: ["التخلي"] },
  // ===== إضافات: سولز لايك =====
  { cat: "سولز لايك", d: 2, q: "وش اسم آلية الإحياء الفوري الخاصة بسيكيرو؟", a: "Resurrection", alt: ["الرزركشن"] },
  { cat: "سولز لايك", d: 2, q: "وش اسم نظام تركيب الأسلحة المميز في Lies of P؟", a: "Weapon Assembly", alt: ["دمج الأسلحة"] },
  { cat: "سولز لايك", d: 2, q: "وش اسم البار اللي تكسره لتضرب ضربة قاضية بسيكيرو وخازان؟", a: "Posture", alt: ["بوستشر"] },
  { cat: "سولز لايك", d: 2, q: "وش السلاح الأشهر بسلسلة بلودبورن اللي يتحول لمنشار؟", a: "Saw Cleaver", alt: ["منشار الصياد"] },
  { cat: "سولز لايك", d: 2, q: "في أي سنة صدرت Elden Ring Nightreign؟", a: "2025" },
  { cat: "سولز لايك", d: 3, q: "كم يوم/ليلة تستمر الجولة الواحدة في Nightreign؟", a: "ثلاثة" },
  { cat: "سولز لايك", d: 3, q: "وش اسم الاستوديو الياباني المطوّر لسلسلة Wo Long؟", a: "Team Ninja", alt: ["تيم نينجا"] },
  { cat: "سولز لايك", d: 3, q: "وش لعبة السولز الصينية اللي بطلها ملك القرود صدرت 2024؟", a: "Black Myth Wukong", alt: ["بلاك ميث ووكونق"] },
  { cat: "سولز لايك", d: 3, q: "من الملحّن الأساسي لموسيقى إلدن رينق ودارك سولز؟", a: "Yuka Kitamura", alt: ["يوكا كيتامورا"] },
  { cat: "سولز لايك", d: 3, q: "وش اسم لعبة السولز الفضائية من فروم سوفتوير؟", a: "Armored Core", alt: ["آرمرد كور"] },
  { cat: "سولز لايك", d: 3, q: "في أي سنة صدرت The First Berserker: Khazan؟", a: "2025" },
  // ===== إضافات: ألعاب فيديو =====
  { cat: "ألعاب فيديو", d: 2, q: "وش اسم النادي/الفصيل اللي ينتمي له بطل Cyberpunk 2077؟", a: "V", alt: ["في"] },
  { cat: "ألعاب فيديو", d: 2, q: "وش أشهر لعبة رعب فيها شخصية بيراميد هيد؟", a: "Silent Hill", alt: ["سايلنت هيل"] },
  { cat: "ألعاب فيديو", d: 2, q: "وش اسم الشركة المطوّرة لسلسلة Resident Evil؟", a: "Capcom", alt: ["كابكوم"] },
  { cat: "ألعاب فيديو", d: 2, q: "وش اسم ابن كريتوس في God of War 2018؟", a: "Atreus", alt: ["أتريوس"] },
  { cat: "ألعاب فيديو", d: 3, q: "وش اسم الذكاء الاصطناعي الشرير في سلسلة Portal؟", a: "GLaDOS", alt: ["غلادوس"] },
  { cat: "ألعاب فيديو", d: 3, q: "وش أول لعبة فيديو تجارية ناجحة بالتاريخ سنة 1972؟", a: "Pong", alt: ["بونق"] },
  { cat: "ألعاب فيديو", d: 3, q: "وش اسم محرك الألعاب الشهير من Epic Games؟", a: "Unreal Engine", alt: ["أنريل إنجن"] },
  { cat: "ألعاب فيديو", d: 3, q: "من مصمم سلسلة Metal Gear Solid؟", a: "Kojima", alt: ["كوجيما"] },
  { cat: "ألعاب فيديو", d: 3, q: "وش اسم أول لعبة صدرت على جهاز نينتندو سويتش يوم إطلاقه؟", a: "Zelda", alt: ["زيلدا"] },
  // ===== إضافات: أوفرواتش =====
  { cat: "أوفرواتش", d: 2, q: "البطلة المصرية اللي تطير وتقصف بالصواريخ؟", a: "Pharah", alt: ["فرح"] },
  { cat: "أوفرواتش", d: 2, q: "البطلة المكسيكية الهاكرة اللي تختفي؟", a: "Sombra", alt: ["سومبرا"] },
  { cat: "أوفرواتش", d: 2, q: "البطل الأسترالي المتفجّر شريك جانكرات؟", a: "Roadhog", alt: ["رودهوغ"] },
  { cat: "أوفرواتش", d: 3, q: "وش اسم الأومنيك الراهب المعالج بالكرات؟", a: "Zenyatta", alt: ["زينياتا"] },
  { cat: "أوفرواتش", d: 3, q: "وش الاسم الحقيقي لويدو ماكر؟", a: "Amelie Lacroix", alt: ["أميلي لاكروا"] },
  { cat: "أوفرواتش", d: 3, q: "في أي سنة صدرت أوفرواتش الأولى؟", a: "2016" },
  { cat: "أوفرواتش", d: 3, q: "وش اسم الهامستر داخل الروبوت الكروي؟", a: "Hammond", alt: ["هامند"] },
  // ===== إضافات: أزياء الشخصيات =====
  { cat: "أزياء الشخصيات", d: 2, q: "بدلة سوداء وقناع أبيض بعيون حمراء وكاتانا، شبح تسوشيما؟", a: "Jin Sakai", alt: ["جين ساكاي"] },
  { cat: "أزياء الشخصيات", d: 2, q: "شعر أبيض طويل ووشمين سيفين على الظهر وعيون صفراء؟", a: "Geralt", alt: ["غيرالت"] },
  { cat: "أزياء الشخصيات", d: 3, q: "درع ثقيل ذهبي وشعر أحمر ناري ومطرقة ضخمة، بوس إلدن رينق؟", a: "Radagon", alt: ["رادغون"] },
  { cat: "أزياء الشخصيات", d: 3, q: "قبعة ساحرة زرقاء وأربع أذرع وجسد دمية، ساحرة إلدن رينق؟", a: "Ranni", alt: ["رانّي"] },
  { cat: "أزياء الشخصيات", d: 3, q: "دمية بمعطف أسود ويد ميكانيكية ووجه شاحب، بطل لعبة كورية؟", a: "P", alt: ["بي"] },
  // ===== إضافات: فيزياء =====
  { cat: "فيزياء", d: 2, q: "وش وحدة قياس المقاومة الكهربائية؟", a: "أوم" },
  { cat: "فيزياء", d: 2, q: "وش القانون اللي يربط الجهد بالتيار والمقاومة؟", a: "قانون أوم" },
  { cat: "فيزياء", d: 2, q: "وش وحدة قياس الطاقة والشغل بالنظام الدولي؟", a: "جول" },
  { cat: "فيزياء", d: 3, q: "كم تبلغ عجلة الجاذبية الأرضية تقريبًا بالمتر لكل ثانية مربعة؟", a: "9.8" },
  { cat: "فيزياء", d: 3, q: "وش القانون اللي ينص أن الطاقة لا تفنى ولا تستحدث؟", a: "حفظ الطاقة" },
  { cat: "فيزياء", d: 3, q: "من العالم صاحب مبدأ عدم اليقين؟", a: "هايزنبرغ" },
  { cat: "فيزياء", d: 3, q: "وش انتقال الحرارة عبر حركة المائع نفسه؟", a: "الحمل" },
  { cat: "فيزياء", d: 3, q: "وش القانون الثاني للديناميكا الحرارية يتكلم عن أي كمية دائمًا تزيد؟", a: "الإنتروبي" },
  // ===== إضافات: علوم =====
  { cat: "علوم", d: 2, q: "وش الغاز اللي تمتصه النباتات بعملية البناء الضوئي؟", a: "ثاني أكسيد الكربون" },
  { cat: "علوم", d: 2, q: "وش أقسى معدن طبيعي معروف؟", a: "الألماس" },
  { cat: "علوم", d: 3, q: "وش عدد كروموسومات الإنسان؟", a: "46" },
  { cat: "علوم", d: 3, q: "من العالم صاحب نظرية التطور بالانتخاب الطبيعي؟", a: "داروين" },
  { cat: "علوم", d: 3, q: "وش أخف عنصر في الجدول الدوري؟", a: "الهيدروجين" },
  { cat: "علوم", d: 3, q: "وش اسم العملية اللي تنقسم فيها الخلية لخليتين متطابقتين؟", a: "الانقسام المتساوي" },
  // ===== إضافات: عام =====
  { cat: "عام", d: 2, q: "كم عدد أضلاع مكعب روبيك على الوجه الواحد؟", a: "تسعة" },
  { cat: "عام", d: 2, q: "وش أسرع حيوان بري في العالم؟", a: "الفهد" },
  { cat: "عام", d: 3, q: "كم عدد القطع في لعبة الشطرنج كاملة؟", a: "32" },
  { cat: "عام", d: 3, q: "كم عدد ضربات القلب الطبيعية بالدقيقة للبالغ تقريبًا؟", a: "70" },
  { cat: "عام", d: 3, q: "وش أطول حيوان بري ارتفاعًا؟", a: "الزرافة" },
  // ===== إضافات: تقنية =====
  { cat: "تقنية", d: 2, q: "وش لغة البرمجة اللي شعارها ثعبان؟", a: "بايثون" },
  { cat: "تقنية", d: 3, q: "وش يعني اختصار GPU بالعربي؟", a: "وحدة معالجة الرسوميات" },
  { cat: "تقنية", d: 3, q: "من مؤسس شركة إنفيديا؟", a: "جينسن هوانغ" },
  { cat: "تقنية", d: 3, q: "وش اسم أول حاسوب إلكتروني عام في التاريخ؟", a: "إنياك" },
  // ===== إضافات: تاريخ وجغرافيا ورياضة =====
  { cat: "تاريخ", d: 3, q: "في أي سنة تأسست الأمم المتحدة؟", a: "1945" },
  { cat: "تاريخ", d: 3, q: "وش المعركة الفاصلة اللي انتهت فيها إمبراطورية نابليون؟", a: "واترلو" },
  { cat: "جغرافيا", d: 3, q: "وش أطول جدار بناه الإنسان؟", a: "سور الصين" },
  { cat: "جغرافيا", d: 3, q: "وش أبرد قارة في العالم؟", a: "أنتاركتيكا" },
  { cat: "رياضة", d: 3, q: "كم عدد الحلقات في شعار الأولمبياد؟", a: "خمس" },
  { cat: "رياضة", d: 3, q: "من أكثر لاعب فاز بالكرة الذهبية؟", a: "ميسي" },

  /* ========== دفعة ثانية — تركيز على الصعب ========== */
  // بوسات السولز
  { cat: "بوسات السولز", d: 1, q: "بوس إلدن رينق الشجرة المتحركة العملاقة بحفرة الأمعاء؟", a: "إلدن ستيم" },
  { cat: "بوسات السولز", d: 2, q: "بوس إلدن رينق الفارس الذهبي وسط الشجرة، القرين المثالي لرادغون؟", a: "غودفري" },
  { cat: "بوسات السولز", d: 2, q: "بوس إلدن رينق التنين العملاق بمزرعة كايليد اللي يقذف الصواعق؟", a: "غرايول" },
  { cat: "بوسات السولز", d: 2, q: "بوس دارك سولز 3 الراقص بالسيوف النارية داخل قلعة لوثريك؟", a: "راقصة الوادي البارد" },
  { cat: "بوسات السولز", d: 2, q: "بوس بلودبورن الخنزير الضخم أو الوحش اللي تواجهه أول اللعبة بالجسر؟", a: "الأب غاسكوين" },
  { cat: "بوسات السولز", d: 2, q: "بوس Lies of P الفارس الأبيض حامل السيف الضخم بالكاتدرائية؟", a: "King's Flame Fuoco" },
  { cat: "بوسات السولز", d: 3, q: "بوس دارك سولز الأولى بالبرج المظلم، الفارس صاحب الرمح والترس المصلوب؟", a: "أورنستين وسموغ" },
  { cat: "بوسات السولز", d: 3, q: "بوس سيكيرو الراهبة الخالدة بقصر الينبوع؟", a: "الراهبة الخالدة" },
  { cat: "بوسات السولز", d: 3, q: "بوس إلدن رينق النبيل الدموي أسفل ليرنيا، ملك الدم؟", a: "موغ" },
  { cat: "بوسات السولز", d: 3, q: "بوس إلدن رينق التنين الميت العملاق بجبل جيلمير؟", a: "فورتساكس" },
  { cat: "بوسات السولز", d: 3, q: "بوس DLC إلدن رينق الأسد الراقص بأول منطقة؟", a: "الأسد الراقص" },
  { cat: "بوسات السولز", d: 3, q: "بوس Nightreign التنين الأزرق العملاق أحد الـ Nightlords؟", a: "فولكانوس" },
  { cat: "بوسات السولز", d: 3, q: "بوس Lies of P الأخ الميكانيكي حامل المطرقة العملاقة بالمصنع؟", a: "Black Rabbit Brotherhood" },
  { cat: "بوسات السولز", d: 3, q: "أول بوس رئيسي بلعبة Khazan، الجنرال الخائن اللي شوّه سمعتك؟", a: "فولباز" },
  { cat: "بوسات السولز", d: 3, q: "بوس دارك سولز 3 الشهير بالمرحلة الثانية الأصعب، سيّاف الرماد النهائي؟", a: "روح الرماد" },
  // لور السولز
  { cat: "لور السولز", d: 2, q: "وش اسم الساحرة القمرية بنت رينالا اللي تعطيك نهاية النجوم؟", a: "Ranni", alt: ["رانّي"] },
  { cat: "لور السولز", d: 2, q: "وش اسم منطقة المستنقعات السامة الحمراء بإلدن رينق؟", a: "بحيرة الفساد" },
  { cat: "لور السولز", d: 3, q: "وش اسم الشعلة الأصلية اللي عمرها من حرب التنانين بدارك سولز؟", a: "النار الأولى" },
  { cat: "لور السولز", d: 3, q: "وش اسم الكنيسة اللي كانت تجري تجارب الدم بيارنام؟", a: "كنيسة الشفاء" },
  { cat: "لور السولز", d: 3, q: "وش اسم الوباء اللي حوّل ناس كرات لمجانين في Lies of P؟", a: "جنون البتروليوم" },
  { cat: "لور السولز", d: 3, q: "وش اسم الأخ غير الشقيق لملكة ماريكا وحاكم ستورمفيل؟", a: "غودريك" },
  { cat: "لور السولز", d: 3, q: "وش اسم الجنرال الأحمر بكايليد اللي يركب الحصان ويقاتلك بمهرجان؟", a: "Radahn", alt: ["رادان"] },
  // سولز لايك
  { cat: "سولز لايك", d: 2, q: "وش نظام Lies of P اللي تختار فيه تكذب أو تصدق ويغيّر النهاية؟", a: "نظام الكذب" },
  { cat: "سولز لايك", d: 2, q: "وش اسم آلية الـ parry الأساسية بلعبة سيكيرو؟", a: "ديفليكت" },
  { cat: "سولز لايك", d: 3, q: "كم عدد الـ Nightlords في Elden Ring Nightreign؟", a: "ثمانية" },
  { cat: "سولز لايك", d: 3, q: "كم يوم داخل اللعبة تستمر الجولة الواحدة في Nightreign؟", a: "ثلاثة أيام" },
  { cat: "سولز لايك", d: 3, q: "وش تسمى آلية شحن سلاحك بالطاقة في Khazan؟", a: "بريكينغ" },
  { cat: "سولز لايك", d: 3, q: "وش اسم الاستوديو الفرعي لـ Nexon اللي طوّر Khazan؟", a: "Neople", alt: ["نيوبل"] },
  { cat: "سولز لايك", d: 3, q: "في أي سنة صدرت Lies of P؟", a: "2023" },
  { cat: "سولز لايك", d: 3, q: "وش اسم اللعبة السولزية الصينية اللي بطلها قرد أسطوري صدرت 2024؟", a: "Black Myth Wukong" },
  { cat: "سولز لايك", d: 3, q: "وش أول لعبة استخدمت مصطلح Soulslike رسميًا كوصف لنوعها؟", a: "دارك سولز" },
  // أوفرواتش
  { cat: "أوفرواتش", d: 1, q: "البطلة اللي تبني جدار جليد وتجمّد الأعداء؟", a: "مي" },
  { cat: "أوفرواتش", d: 2, q: "البطل الملك الميكانيكي بموسيقى الروك وقيثارة؟", a: "لوسيو" },
  { cat: "أوفرواتش", d: 2, q: "البطلة القناصة الفرنسية بالبشرة الزرقاء؟", a: "ويدوميكر" },
  { cat: "أوفرواتش", d: 2, q: "البطل المصري الملك الأسطوري صاحب الدرع المضاد؟", a: "سيغما" },
  { cat: "أوفرواتش", d: 3, q: "وش اسم بطلة الهاكر المكسيكية؟", a: "Sombra", alt: ["سومبرا"] },
  { cat: "أوفرواتش", d: 3, q: "وش اسم البطلة المصرية اللي تطير وتطلق الصواريخ؟", a: "فارا" },
  { cat: "أوفرواتش", d: 3, q: "وش الاسم الحقيقي لبطلة دي فا الكورية؟", a: "هانا سونغ" },
  // أزياء الشخصيات
  { cat: "أزياء الشخصيات", d: 1, q: "بدلة برتقالية وقبعة قش بالها، قرصان يبي يصير ملك القراصنة؟", a: "لوفي" },
  { cat: "أزياء الشخصيات", d: 2, q: "بدلة سوداء ضيقة وقناع أبيض بفتحتين وشعر أحمر، دمية كورية بسيف؟", a: "خازان" },
  { cat: "أزياء الشخصيات", d: 2, q: "قبعة سوداء عريضة وشعر أبيض طويل وعينين صفراوين وسيفين على الظهر؟", a: "Geralt", alt: ["غيرالت"] },
  { cat: "أزياء الشخصيات", d: 2, q: "معطف جلدي أحمر طويل وشعر أبيض وسيف ومسدسين، صائد شياطين؟", a: "دانتي" },
  { cat: "أزياء الشخصيات", d: 3, q: "بدلة سوداء وقناع نصف ميكانيكي وذراع صناعية وسيف كاتانا في اليابان القديمة؟", a: "Sekiro", alt: ["سيكيرو"] },
  { cat: "أزياء الشخصيات", d: 3, q: "دمية بربطة عنق وقبعة صغيرة وذراع ميكانيكية قابلة للتبديل بمدينة كرات؟", a: "بينوكيو" },
  { cat: "أزياء الشخصيات", d: 3, q: "معطف أزرق وشعر أشقر مصفف وشارة محقق في مدينة يابانية معاصرة؟", a: "كيريو كازوما" },
  // ألعاب فيديو
  { cat: "ألعاب فيديو", d: 2, q: "وش اسم الوكيل المغربي في Valorant اللي يسوي جدران؟", a: "سايفا" },
  { cat: "ألعاب فيديو", d: 2, q: "وش اسم اللعبة اللي بطلها جيرالت الساحر؟", a: "ذا ويتشر" },
  { cat: "ألعاب فيديو", d: 2, q: "وش الشركة اللي تطوّر سلسلة Assassin's Creed؟", a: "يوبي سوفت" },
  { cat: "ألعاب فيديو", d: 3, q: "وش اسم أول وكيل عربي انضم لـ Valorant؟", a: "سايفا" },
  { cat: "ألعاب فيديو", d: 3, q: "في أي سنة صدرت لعبة Half-Life 2؟", a: "2004" },
  { cat: "ألعاب فيديو", d: 3, q: "وش اسم المحرك الرسومي اللي تستخدمه Epic Games؟", a: "Unreal Engine", alt: ["أنريل إنجن"] },
  { cat: "ألعاب فيديو", d: 3, q: "وش اسم الجزيرة اللي تدور فيها أحداث Far Cry 3؟", a: "روك" },
  // فيزياء
  { cat: "فيزياء", d: 2, q: "وش وحدة قياس الشغل والطاقة في النظام الدولي؟", a: "جول" },
  { cat: "فيزياء", d: 2, q: "وش ظاهرة انكسار الضوء لما يمر من وسط لوسط آخر تسمى؟", a: "الانكسار" },
  { cat: "فيزياء", d: 3, q: "وش قانون أوم اللي يربط الجهد بالتيار والمقاومة؟", a: "V=IR" },
  { cat: "فيزياء", d: 3, q: "وش قيمة تسارع الجاذبية الأرضية تقريبًا بالمتر لكل ثانية مربعة؟", a: "9.8" },
  { cat: "فيزياء", d: 3, q: "وش القانون اللي ينص أن الطاقة لا تفنى ولا تُستحدث؟", a: "حفظ الطاقة" },
  { cat: "فيزياء", d: 3, q: "من العالم صاحب مبدأ عدم اليقين في ميكانيكا الكم؟", a: "هايزنبرغ" },
  { cat: "فيزياء", d: 3, q: "وش وحدة قياس الكمية الكهربائية (الشحنة)؟", a: "كولوم" },
  { cat: "فيزياء", d: 3, q: "وش الظاهرة اللي تفسر تغيّر تردد الصوت مع حركة المصدر؟", a: "دوبلر" },
  // علوم
  { cat: "علوم", d: 2, q: "وش العملية اللي تصنع فيها النباتات غذاءها من الضوء؟", a: "التمثيل الضوئي" },
  { cat: "علوم", d: 3, q: "وش العنصر اللي رمزه Au؟", a: "الذهب" },
  { cat: "علوم", d: 3, q: "كم عدد الكروموسومات في خلية الإنسان الطبيعية؟", a: "46" },
  { cat: "علوم", d: 3, q: "وش اسم أطول عظمة في جسم الإنسان؟", a: "عظمة الفخذ" },
  { cat: "علوم", d: 3, q: "وش الغاز المسؤول الأكبر عن الاحتباس الحراري؟", a: "ثاني أكسيد الكربون" },
  // عام
  { cat: "عام", d: 3, q: "كم عدد دول العالم المعترف بها في الأمم المتحدة؟", a: "193" },
  { cat: "عام", d: 3, q: "وش أثقل حيوان على وجه الأرض؟", a: "الحوت الأزرق" },
  { cat: "عام", d: 3, q: "كم عدد الأضلاع في جسم الإنسان؟", a: "24" },
  // رياضة
  { cat: "رياضة", d: 3, q: "كم عدد بطولات دوري أبطال أوروبا لريال مدريد حتى 2025؟", a: "15" },
  { cat: "رياضة", d: 3, q: "في أي سنة فازت السعودية على الأرجنتين بكأس العالم؟", a: "2022" },
  { cat: "رياضة", d: 3, q: "من أكثر لاعب فاز بالكرة الذهبية في التاريخ؟", a: "ميسي" },
  // أفلام
  { cat: "أفلام ومسلسلات", d: 3, q: "وش اسم شخصية والتر وايت المستعارة في Breaking Bad؟", a: "هايزنبرغ" },
  { cat: "أفلام ومسلسلات", d: 3, q: "من مخرج فيلم Pulp Fiction؟", a: "تارانتينو" },
  { cat: "أفلام ومسلسلات", d: 3, q: "وش اسم الفندق في فيلم The Shining؟", a: "أوفرلوك" },
  // تاريخ
  { cat: "تاريخ", d: 3, q: "في أي سنة هجرية وقعت معركة بدر؟", a: "2 هجرية" },
  { cat: "تاريخ", d: 3, q: "من أول من وضع التقويم الهجري؟", a: "عمر بن الخطاب" },
  { cat: "تاريخ", d: 3, q: "في أي سنة فتح المسلمون القسطنطينية؟", a: "1453" },
  // جغرافيا
  { cat: "جغرافيا", d: 3, q: "وش أعلى قمة جبلية في العالم العربي؟", a: "جبل توبقال" },
  { cat: "جغرافيا", d: 3, q: "وش أطول حدود برية بين دولتين في العالم؟", a: "كندا وأمريكا" },
  { cat: "جغرافيا", d: 3, q: "كم عدد مناطق المملكة العربية السعودية الإدارية؟", a: "13" },
  // تقنية
  { cat: "تقنية", d: 3, q: "وش يعني اختصار HTTP؟", a: "بروتوكول نقل النص التشعبي" },
  { cat: "تقنية", d: 3, q: "من مخترع الشبكة العنكبوتية العالمية؟", a: "تيم بيرنرز لي" },
  { cat: "تقنية", d: 3, q: "وش أول لغة برمجة عالية المستوى؟", a: "فورتران" },
  { cat: "تقنية", d: 3, q: "كم بت في البايت الواحد؟", a: "8" },
];

/* ---------- مولد الرياضيات ---------- */
const rnd = (lo, hi) => lo + Math.floor(Math.random() * (hi - lo + 1));
function genMathQ(d) {
  let text, ans;
  if (d === 1) {
    const x = rnd(12, 89), y = rnd(12, 89);
    if (Math.random() < 0.5) { text = `${x} + ${y}`; ans = x + y; }
    else { const a2 = Math.max(x, y), b2 = Math.min(x, y); text = `${a2} − ${b2}`; ans = a2 - b2; }
  } else if (d === 2) {
    if (Math.random() < 0.5) { const x = rnd(12, 25), y = rnd(3, 9); text = `${x} × ${y}`; ans = x * y; }
    else { const x = rnd(20, 90), y = rnd(3, 9), z = rnd(11, 40); text = `${z} + ${x} × ${y}`; ans = z + x * y; }
  } else {
    if (Math.random() < 0.5) {
      const p = [10, 15, 20, 25, 40, 60, 75][rnd(0, 6)];
      const N = rnd(2, 12) * 40; text = `${p}% من ${N}`; ans = Math.round((p / 100) * N);
    } else { const x = rnd(11, 19), y = rnd(11, 19); text = `${x} × ${y}`; ans = x * y; }
  }
  return { type: "typed", exact: true, cat: "رياضيات", d, q: "كم ناتج: " + text + "؟", a: String(ans) };
}

/* ---------- أدوات التخزين ---------- */
const jset = async (k, v, shared = true) => {
  try { await window.storage.set(k, JSON.stringify(v), shared); return true; } catch (e) { return false; }
};
const jget = async (k, shared = true) => {
  try { const r = await window.storage.get(k, shared); return r ? JSON.parse(r.value) : null; } catch (e) { return null; }
};

/* ---------- الذكاء الاصطناعي ---------- */
async function askClaude(prompt) {
  const ctrl = new AbortController();
  const killer = setTimeout(() => ctrl.abort(), 9000); // ما ننتظر أكثر من 9 ثواني
  try {
    return await askClaudeInner(prompt, ctrl.signal);
  } finally {
    clearTimeout(killer);
  }
}

async function askClaudeInner(prompt, signal) {
  const res = await fetch("/api/claude", {
    signal,
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ prompt }),
  });
  const data = await res.json();
  if (!res.ok) throw new Error(data?.error || "AI request failed");
  return String(data.text || "").replace(/```json|```/g, "").trim();
}

async function genTyped(cats, n) {
  const prompt = `أنت مولّد أسئلة لمجموعة أصدقاء سعوديين قيمرز يحبون التحدي. ولّد ${n} أسئلة مبتكرة وغير مستهلكة بالعربية إجابتها كلمة أو كلمتين أو رقم (بدون خيارات). الفئات: ${cats.join("، ")}. تجنب الأسئلة الكليشيه، واستخدم تلميحات ذكية أو تفاصيل غير مشهورة، ونوّع الصعوبة (1 سهل، 2 متوسط، 3 صعب). تأكد أن الإجابة واحدة مؤكدة لا لبس فيها. أرجع JSON فقط بدون أي نص آخر: [{"q":"نص السؤال","a":"الإجابة","d":2,"cat":"الفئة"}]`;
  const raw = await askClaude(prompt);
  const arr = JSON.parse(raw);
  return arr.filter((x) => x.q && x.a)
    .map((x) => ({ type: "typed", q: x.q, a: String(x.a), d: x.d >= 1 && x.d <= 3 ? x.d : 2, cat: x.cat || cats[0] }));
}

const AR_NUM = {
  "صفر": "0", "واحد": "1", "وحده": "1", "اثنين": "2", "اثنان": "2", "ثنين": "2",
  "ثلاثه": "3", "ثلاث": "3", "اربعه": "4", "اربع": "4", "خمسه": "5", "خمس": "5",
  "سته": "6", "ست": "6", "سبعه": "7", "سبع": "7", "ثمانيه": "8", "ثمان": "8",
  "تسعه": "9", "تسع": "9", "عشره": "10", "عشر": "10", "احدعشر": "11", "اثناعشر": "12",
  "مئه": "100", "مايه": "100", "الف": "1000",
};
const STOP = new Set(["ال", "من", "في", "على", "عن", "الى", "هو", "هي", "بن", "ابو", "ام", "the", "of", "a", "an"]);

const norm = (s) => (s || "").toString().trim().toLowerCase()
  .replace(/[٠-٩]/g, (d) => "٠١٢٣٤٥٦٧٨٩".indexOf(d))
  .replace(/[أإآٱ]/g, "ا").replace(/[ةه]/g, "ه").replace(/[ىي]/g, "ي")
  .replace(/ؤ/g, "و").replace(/ئ/g, "ي").replace(/ء/g, "")
  .replace(/[\u064B-\u0652\u0640]/g, "")
  .replace(/[^\p{L}\p{N}\s]/gu, " ")
  .replace(/\s+/g, " ").trim();

const tokens = (s) => norm(s).split(" ")
  .map((w) => (w.length > 3 && w.startsWith("ال") ? w.slice(2) : w))
  .map((w) => AR_NUM[w] || w)
  .filter((w) => w && !STOP.has(w));

const normNum = (s) => {
  const t = norm(s);
  const m = t.replace(/\s/g, "").match(/-?\d+(\.\d+)?/);
  if (m) return m[0];
  const w = tokens(s).find((x) => AR_NUM[x] || /^\d+$/.test(x));
  return w ? (AR_NUM[w] || w) : null;
};

// مسافة ليفنشتاين للتسامح مع الأخطاء الإملائية
function lev(a, b) {
  if (a === b) return 0;
  const m = a.length, n = b.length;
  if (!m || !n) return Math.max(m, n);
  let prev = Array.from({ length: n + 1 }, (_, i) => i);
  for (let i = 1; i <= m; i++) {
    const cur = [i];
    for (let j = 1; j <= n; j++) {
      cur[j] = Math.min(prev[j] + 1, cur[j - 1] + 1, prev[j - 1] + (a[i - 1] === b[j - 1] ? 0 : 1));
    }
    prev = cur;
  }
  return prev[n];
}
const close = (a, b) => {
  if (!a || !b) return false;
  if (a === b) return true;
  const d = lev(a, b), L = Math.max(a.length, b.length);
  return d <= (L <= 4 ? 1 : L <= 8 ? 2 : 3) && 1 - d / L >= 0.75;
};


// ---- مطابقة صوتية بين العربي والإنجليزي ----
// نحوّل الاسم لهيكل حروف ساكنة، فيتطابق "ماليينيا" مع "Malenia" تلقائيًا
const AR_SKEL = {
  "ب": "b", "ت": "t", "ث": "t", "ج": "g", "ح": "h", "خ": "k", "د": "d", "ذ": "z",
  "ر": "r", "ز": "z", "س": "s", "ش": "s", "ص": "s", "ض": "d", "ط": "t", "ظ": "z",
  "ع": "", "غ": "g", "ف": "f", "ق": "k", "ك": "k", "ل": "l", "م": "m", "ن": "n",
  "ه": "h", "و": "", "ي": "", "ا": "", "ء": "", "ة": "h", "ى": "", "پ": "b", "ڤ": "f", "چ": "j", "ژ": "z", "گ": "g",
};
function skeleton(s) {
  let t = norm(s);
  if (!t) return "";
  // إنجليزي: ثنائيات الحروف أولاً ثم حذف الحركات
  t = t.replace(/^ال/, "").replace(/ ال/g, " ");           // أل التعريف
  t = t.replace(/ph/g, "f").replace(/gh/g, "g").replace(/kh/g, "k")
       .replace(/sh/g, "s").replace(/ch/g, "s").replace(/th/g, "t")
       .replace(/dh/g, "d").replace(/ck/g, "k").replace(/qu/g, "k")
       .replace(/x/g, "ks").replace(/c([eiy])/g, "s$1")        // ce/ci/cy = س
       .replace(/c/g, "k").replace(/q/g, "k").replace(/v/g, "f")
       .replace(/p/g, "b").replace(/j/g, "g")                  // ج تُنطق g أو j
       .replace(/[aeiouwy]/g, "");
  let out = "";
  for (const ch of t) {
    if (AR_SKEL[ch] !== undefined) out += AR_SKEL[ch];
    else if (/[a-z0-9]/.test(ch)) out += ch;
    else if (ch === " ") out += " ";
  }
  // حذف التكرار المتجاور: "bb" -> "b"
  return out.replace(/(.)\1+/g, "$1").replace(/\s+/g, " ").trim();
}
function skelMatch(a, b) {
  const x = skeleton(a).replace(/\s/g, ""), y = skeleton(b).replace(/\s/g, "");
  if (!x || !y) return false;
  if (x === y) return x.length >= 2;
  if (x.length < 3 || y.length < 3) return false;
  const d = lev(x, y), L = Math.max(x.length, y.length);
  return d <= (L <= 5 ? 1 : 2) && 1 - d / L >= 0.7;
}

// التصحيح المحلي: يشتغل بدون إنترنت أو حساب
function localJudge(userText, modelAnswer, alts) {
  // نقبل الإجابة النموذجية أو أي بديل مسجل (عربي/إنجليزي)
  const list = [modelAnswer, ...(alts || [])].filter(Boolean);
  if (list.length > 1) return list.some((a) => judgeOne(userText, a));
  return judgeOne(userText, modelAnswer);
}

function judgeOne(userText, modelAnswer) {
  const u = norm(userText), m = norm(modelAnswer);
  if (!u) return false;
  if (u === m) return true;
  if (skelMatch(userText, modelAnswer)) return true; // عربي ضد إنجليزي
  // أرقام
  const un = normNum(userText), mn = normNum(modelAnswer);
  if (mn !== null && un !== null && un === mn) return true;
  if (mn !== null && un === null) return false;
  const ut = tokens(userText), mt = tokens(modelAnswer);
  if (!ut.length || !mt.length) return false;
  const uj = ut.join(" "), mj = mt.join(" ");
  if (uj === mj) return true;
  if (close(uj, mj)) return true;
  // كل كلمة مهمة بالإجابة النموذجية موجودة (أو قريبة) عند اللاعب
  const covered = mt.every((mw) => ut.some((uw) => close(uw, mw) || skelMatch(uw, mw)));
  if (covered) return true;
  // إجابة جزئية مقبولة: كل كلمة كتبها اللاعب موجودة بالإجابة، وفيها كلمة مميزة
  if (mt.length > 1) {
    const allInModel = ut.every((uw) => mt.some((mw) => close(uw, mw)));
    const hasKey = ut.some((uw) => uw.length >= 3 && mt.some((mw) => close(uw, mw)));
    if (allInModel && hasKey) return true;
  }
  return false;
}

async function judgeTyped(question, modelAnswer, entries, useAI, alts) {
  // 1) التصحيح المحلي أولاً — يشتغل دائمًا وبدون حساب
  const map = {};
  const unsure = [];
  entries.forEach((en) => {
    const ok = localJudge(en.text, modelAnswer, alts);
    map[en.pid] = ok;
    if (!ok) unsure.push(en); // المرفوضة محليًا نعطيها فرصة ثانية مع AI
  });
  if (!useAI || !unsure.length) return map;
  // 2) الـ AI يراجع المرفوضة بس (مرادفات، أسماء بديلة، صياغة مختلفة)
  try {
    const prompt = `احكم على إجابات لاعبين في لعبة أسئلة عربية. السؤال: "${question}". الإجابة النموذجية: "${modelAnswer}". اقبل الإجابة إذا كانت صحيحة المعنى حتى لو اختلفت الصياغة، أو كانت مرادفًا أو اسمًا بديلاً معروفًا، أو كُتبت بالإنجليزي بدل العربي أو العكس. ارفضها إذا كانت غلط. أرجع JSON فقط: [{"pid":"...","ok":true}]. الإجابات: ${JSON.stringify(unsure)}`;
    const raw = await askClaude(prompt);
    JSON.parse(raw).forEach((v) => { if (v && v.pid && v.ok) map[v.pid] = true; });
  } catch (e) { /* نكتفي بالتصحيح المحلي */ }
  return map;
}

/* ---------- عناصر مساعدة ---------- */
const shuffle = (a) => { const b = [...a]; for (let i = b.length - 1; i > 0; i--) { const j = Math.floor(Math.random() * (i + 1)); [b[i], b[j]] = [b[j], b[i]]; } return b; };
const genCode = () => { const cs = "ABCDEFGHJKMNPQRSTUVWXYZ23456789"; let s = ""; for (let i = 0; i < 4; i++) s += cs[Math.floor(Math.random() * cs.length)]; return s; };
const genPid = () => "p" + Math.random().toString(36).slice(2, 8);
const MAX_SLOTS = 8;
const roomKey = (c) => `fz:room:${c}`;
const slotKey = (c, s) => `fz:s${s}:${c}`;   // مفتاح ثابت لكل لاعب — ما نحتاج list()
const isEventSlot = (idx) => idx > 0 && idx % 3 === 2;
const INVENTIVE = ["أعلام", "شكل ورسم", "مين البوس؟", "وين المكان؟", "مين قالها؟", "وش الغرض؟",
  "الرابط المشترك", "قبل ولا بعد؟", "إيموجي", "دليلين", "الأغرب", "لو كنت مكانك"];
const CLASSIC = CATS.filter((c) => !INVENTIVE.includes(c));
// نضمن 4 فئات مبتكرة + 2 كلاسيكية كل جولة
const sampleCats = () => shuffle([...shuffle(INVENTIVE).slice(0, 4), ...shuffle(CLASSIC).slice(0, 2)]);

const Sadu = () => (
  <div className="sadu" aria-hidden="true">
    <div className="zz" style={{ "--c": "#D9494F" }} />
    <div className="zz" style={{ "--c": "#F0A32F" }} />
    <div className="zz" style={{ "--c": "#2EC4A6" }} />
  </div>
);

/* ============================================================ */
export default function App() {
  const [screen, setScreen] = useState("home");
  const [role, setRole] = useState(null);
  const [me, setMe] = useState({ pid: genPid(), name: "" });
  const [code, setCode] = useState("");
  const [view, setView] = useState(null);
  const lastVerRef = useRef(0);
  const offsetRef = useRef(0); // فرق التوقيت بين جهاز اللاعب والهوست
  const viewRef = useRef(null);
  const offSamplesRef = useRef([]);
  const mySlotRef = useRef(null);
  const slotDataRef = useRef({});
  const useCounterRef = useRef(0);
  const [myq, setMyq] = useState([]);
  const [toast, setToast] = useState("");
  const [loading, setLoading] = useState("");
  const [, forceN] = useState(0);
  const rerender = () => forceN((x) => x + 1);

  const [cfg, setCfg] = useState({ count: 25, src: { bank: true, ai: true, mine: false } });

  const localRef = useRef({ qIndex: -1, renderAt: 0, answered: false, text: "" });
  const [typedText, setTypedText] = useState("");
  const [myVote, setMyVote] = useState(null);
  const [pendingItem, setPendingItem] = useState(null);
  const lastCatQRef = useRef(-1);
  const [, setTick] = useState(0);

  const hostRef = useRef(null);

  const [storeOk, setStoreOk] = useState("checking"); // checking | ok | unknown

  useEffect(() => {
    (async () => {
      // فحص التخزين: متسامح — نعتبره سليمًا إلا لو فشلت الكتابة والقراءة معًا
      let ok = false;
      const probe = "fz:probe:" + Math.random().toString(36).slice(2, 8);
      try {
        await window.storage.set(probe, "1", true);
        ok = true; // ما رمت استثناء = الكتابة تمت
      } catch (e) { ok = false; }
      if (!ok) {
        try {
          const r = await window.storage.get(probe, true);
          if (r) ok = true;
        } catch (e) { /* لا شيء */ }
      }
      try { await window.storage.delete(probe, true); } catch (e) { /* غير مهم */ }
      setStoreOk(ok ? "ok" : "unknown");
    })();
  }, []);

  useEffect(() => {
    (async () => {
      const savedMe = await jget("fz:me", false);
      if (savedMe && savedMe.pid) setMe(savedMe);
      const savedQ = await jget("fz:myq", false);
      if (Array.isArray(savedQ)) setMyq(savedQ.filter((x) => x.a));
    })();
  }, []);

  useEffect(() => {
    if (!toast) return;
    const t = setTimeout(() => setToast(""), 3500);
    return () => clearTimeout(t);
  }, [toast]);

  // نبض العدّاد — يشتغل فقط أثناء السؤال (سلاسة أعلى بباقي الشاشات)
  useEffect(() => {
    if (!view || !["question", "reveal", "event", "catpick"].includes(view.phase)) return;
    const fast = view.phase === "question";
    const t = setInterval(() => setTick((x) => x + 1), fast ? 100 : 300);
    return () => clearInterval(t);
  }, [view && view.phase]);

  /* ---------- اختيار سؤال حسب الفئة والجولة ---------- */
  async function questionFor(cat, idx) {
    const h = hostRef.current;
    const total = h.total;
    // منحنى صعوبة حاد: إحماء قصير، ثم متوسط سريع، وأغلب اللعبة صعب
    const p = total > 1 ? idx / (total - 1) : 0;
    const dT = p < 0.12 ? 1 : p < 0.3 ? 2 : 3;
    // النقاط ترتفع كل ما تقدمنا: ×1 → ×1.5 → ×2 → ×3 بالربع الأخير
    const heat = p < 0.3 ? 1 : p < 0.55 ? 1.5 : p < 0.8 ? 2 : 3;
    h.heat = heat;
    const scale = (d) => Math.round((PTS[d] * heat) / 50) * 50;
    if (cat === "رياضيات") {
      const q = genMathQ(dT);
      return { ...q, pts: scale(q.d) };
    }
    let pool = [];
    if (cfg.src.mine) pool = pool.concat(myq.filter((q) => q.cat === cat).map((q) => ({ type: "typed", ...q })));
    if (cfg.src.bank) pool = pool.concat(BANK.filter((b) => b.cat === cat).map((b) => ({ type: "typed", ...b })));
    pool = pool.filter((q) => !h.used.has(q.q));
    let cand = pool.filter((q) => q.d === dT);
    if (!cand.length) cand = pool;
    if (!cand.length && cfg.src.ai) {
      try { const g = await genTyped([cat], 1); if (g.length) cand = g.filter((q) => !h.used.has(q.q)); } catch (e) { /* fallback */ }
    }
    if (!cand.length) cand = BANK.map((b) => ({ type: "typed", ...b })).filter((q) => !h.used.has(q.q));
    if (!cand.length) cand = [genMathQ(dT)];
    const q = cand[Math.floor(Math.random() * cand.length)];
    h.used.add(q.q);
    return { ...q, pts: scale(q.d) || 400 };
  }


  /* ---------- الآيتمات ---------- */
  function applyItemUse(userPid, itemId, targetPid) {
    const h = hostRef.current;
    if (!h) return false;
    const inv = h.items[userPid] || [];
    const idx = inv.indexOf(itemId);
    if (idx === -1) return false;
    const it = itemById(itemId);
    if (!it) return false;
    const victim = it.target ? targetPid : userPid;
    if (!victim || !h.players[victim]) return false;
    if (it.target && victim === userPid) return false;
    inv.splice(idx, 1);
    h.items[userPid] = inv;
    const userName = h.players[userPid] ? h.players[userPid].name : "لاعب";
    // الدرع يصد أي تخريب
    const vfx = h.fx[victim] || [];
    if (it.target && vfx.includes("shield")) {
      h.itemLog.push({ icon: "🛡️", text: `${h.players[victim].name} صدّ ${it.name} من ${userName}!` });
      return true;
    }
    h.fx[victim] = [...vfx, itemId];
    h.itemLog.push({
      icon: it.icon,
      text: it.target ? `${userName} رمى ${it.name} على ${h.players[victim].name}` : `${userName} رفع درعه`,
    });
    return true;
  }

  function grantItem(pid) {
    const h = hostRef.current;
    if (!h || !h.players[pid]) return null;
    const it = ITEMS[Math.floor(Math.random() * ITEMS.length)];
    h.items[pid] = [...(h.items[pid] || []), it.id];
    return it;
  }

  async function useItem(itemId, targetPid) {
    if (role === "host") {
      const ok = applyItemUse(me.pid, itemId, targetPid);
      if (ok) { await broadcast(); rerender(); }
    } else {
      useCounterRef.current += 1;
      await pushSlot({ use: { q: view.qIndex, item: itemId, target: targetPid, n: useCounterRef.current } }, true);
      setToast("أرسلنا الآيتم ⚡");
    }
  }

  /* ---------- الهوست: بث الحالة ---------- */
  function durFor(h) {
    // الوقت يقصر كل ما تقدمنا: 40 ث بالبداية تنزل لـ 16 ث بالنهاية
    const p = h.total > 1 ? h.qIndex / (h.total - 1) : 0;
    const base = Math.round(TYPED_SEC - p * 24);
    return h.currentEvent && h.currentEvent.id === "blitz" ? Math.round(base / 2) : base;
  }

  async function broadcast() {
    const h = hostRef.current;
    if (!h) return;
    h.ver = (h.ver || 0) + 1;
    const board = Object.entries(h.players)
      .map(([pid, p]) => ({ pid, name: p.name, score: p.score }))
      .sort((a, b) => b.score - a.score);
    const q = h.questions[h.qIndex];
    const pub = {
      code: h.code, phase: h.phase, qIndex: h.qIndex, total: h.total,
      board, judging: !!h.judging, reveal: h.reveal || null, v: h.ver,
      answeredPids: Object.keys(h.answers || {}),
      event: ["event", "question", "closing"].includes(h.phase) ? h.currentEvent || null : null,
      chosenCat: h.chosenCat || null,
      items: h.items || {}, fx: h.fx || {}, itemLog: h.itemLog || [],
      canUseItems: ["catpick", "event"].includes(h.phase),
      stageStart: h.stageStart || 0, catStart: h.catStart || 0, autoNext: h.autoNext !== false, qStartAt: h.qStart || 0,
      cm: h.phase === "catpick" ? {
        mode: h.catMode, options: h.catOptions,
        pickerPid: h.pickerPid, pickerName: h.pickerPid && h.players[h.pickerPid] ? h.players[h.pickerPid].name : "",
        voters: (() => {
          const v = {};
          Object.entries(h.votes || {}).forEach(([pid, cat]) => {
            if (!v[cat]) v[cat] = [];
            v[cat].push(h.players[pid] ? h.players[pid].name : "لاعب");
          });
          return v;
        })(),
        voted: Object.keys(h.votes || {}).length,
        totalPlayers: Object.keys(h.players).length,
      } : null,
      updatedAt: Date.now(),
      q: h.phase === "question" && q ? { q: q.q, pts: q.pts, d: q.d, cat: q.cat, dur: durFor(h) } : null,
    };
    setView(pub); // تحديث فوري للشاشة ثم الحفظ بالخلفية
    await jset(roomKey(h.code), pub);
  }

  /* ---------- الهوست: مزامنة ---------- */
  useEffect(() => {
    if (role !== "host" || screen !== "game") return;
    let busy = false;
    const iv = setInterval(async () => {
      const h = hostRef.current;
      if (!h || busy) return;
      if (!["lobby", "question", "catpick", "event", "reveal"].includes(h.phase)) return;
      busy = true;
      try {
        // ترشيد الطلبات: مسح شامل نادر، وبعده نقرأ الخانات المشغولة فقط
        h.scanTick = (h.scanTick || 0) + 1;
        const fullScan = h.phase === "lobby" || h.scanTick % 6 === 1 || !h.liveSlots || !h.liveSlots.length;
        const toRead = fullScan
          ? Array.from({ length: MAX_SLOTS }, (_, i) => i + 1)
          : h.liveSlots;
        const slots = await Promise.all(toRead.map((n) => jget(slotKey(h.code, n))));
        if (fullScan) {
          h.liveSlots = toRead.filter((n, i) => slots[i] && slots[i].pid);
        }
        for (const a of slots) {
          if (!a || !a.pid || a.pid === me.pid) continue;
          if (Date.now() - (a.t || 0) > 120000) continue; // خانة قديمة
          if (!h.players[a.pid]) h.players[a.pid] = { name: a.name || "لاعب", score: 0 };
          if (a.name) h.players[a.pid].name = a.name;
          h.seen[a.pid] = Date.now();
          if (a.ans && a.ans.q === h.qIndex && !(a.pid in h.answers)) {
            h.answers[a.pid] = { text: a.ans.text, ms: a.ans.ms || 0 };
          }
          if (a.vote && a.vote.q === h.qIndex && a.vote.cat) {
            h.votes[a.pid] = a.vote.cat;
          }
          if (a.use && a.use.q === h.qIndex) {
            const tag = a.pid + ":" + a.use.n;
            if (!h.doneUses) h.doneUses = {};
            if (!h.doneUses[tag]) {
              h.doneUses[tag] = 1;
              applyItemUse(a.pid, a.use.item, a.use.target);
            }
          }
        }
        if (h.phase === "question") {
          // كل من له خانة يُعتبر لاعبًا حاضرًا (ولو تأخر نبضه)
          const active = Object.keys(h.players).filter(
            (pid) => pid === me.pid || Date.now() - (h.seen[pid] || 0) < 90000
          );
          const allAnswered = active.length > 0 && active.every((pid) => pid in h.answers);
          const sinceStart = Date.now() - h.qStart;
          // نمهل 3 ثواني بعد البداية قبل ما نسمح بالإقفال المبكر (وقت وصول الإجابات)
          const mayCloseEarly = allAnswered && sinceStart > 3000;
          const timeUp = Date.now() > h.qStart + durFor(h) * 1000 + 4000;
          if (mayCloseEarly || timeUp) { await closeQuestion(); busy = false; return; }
        }
        // انتقال تلقائي عشان ما ينتظر أحد الهوست
        if (h.autoNext !== false && h.phase === "reveal" && Date.now() - (h.stageStart || 0) > REVEAL_SEC * 1000) {
          await advanceTo(h.qIndex + 1); busy = false; return;
        }
        if (h.autoNext !== false && h.phase === "event" && Date.now() - (h.stageStart || 0) > EVENT_SEC * 1000) {
          await beginQuestion(h.qIndex); busy = false; return;
        }
        if (h.phase === "catpick") {
          const elapsed = Date.now() - h.catStart;
          if (h.catMode === "vote") {
            const pids = Object.keys(h.players);
            const allVoted = pids.length > 0 && pids.every((pid) => h.votes[pid]);
            if (allVoted || elapsed > VOTE_SEC * 1000) {
              await resolveCat(tallyVotes(h)); busy = false; return;
            }
          } else {
            const pv = h.votes[h.pickerPid];
            if (pv) { await resolveCat(pv); busy = false; return; }
            if (elapsed > PICK_SEC * 1000) {
              await resolveCat(h.catOptions[rnd(0, h.catOptions.length - 1)]); busy = false; return;
            }
          }
        }
        await broadcast();
      } catch (e) { /* نحاول بالدورة الجاية */ }
      busy = false;
      rerender();
    }, 1400);
    return () => clearInterval(iv);
  }, [role, screen]);

  function tallyVotes(h) {
    const counts = {};
    Object.values(h.votes).forEach((c) => { counts[c] = (counts[c] || 0) + 1; });
    let top = [], max = 0;
    for (const [c, n] of Object.entries(counts)) {
      if (n > max) { max = n; top = [c]; } else if (n === max) top.push(c);
    }
    return top.length ? top[rnd(0, top.length - 1)] : h.catOptions[0];
  }

  async function resolveCat(cat) {
    const h = hostRef.current;
    if (!h || h.phase !== "catpick") return;
    h.phase = "prep";
    h.chosenCat = cat;
    h.prepStart = Date.now();
    await broadcast();
    // حارس: لو تأخر التجهيز لأي سبب، نأخذ سؤالًا من البنك فورًا
    let q;
    try {
      q = await Promise.race([
        questionFor(cat, h.qIndex),
        new Promise((res) => setTimeout(() => res(null), 11000)),
      ]);
    } catch (e) { q = null; }
    if (!q) {
      const pool = BANK.filter((b) => !h.used.has(b.q));
      const pick = (pool.length ? pool : BANK)[Math.floor(Math.random() * (pool.length || BANK.length))];
      h.used.add(pick.q);
      q = { type: "typed", ...pick, pts: PTS[pick.d] || 400 };
    }
    h.questions[h.qIndex] = q;
    if (isEventSlot(h.qIndex)) {
      h.currentEvent = pickEvent(h);
      if (h.currentEvent.id === "roulette") h.rolled = rnd(1, 10) * 100;
      h.phase = "event";
      h.stageStart = Date.now();
      await broadcast();
      rerender();
    } else {
      h.currentEvent = null;
      await beginQuestion(h.qIndex);
    }
  }

  /* ---------- الهوست: إغلاق السؤال والتحكيم ---------- */
  async function closeQuestion() {
    const h = hostRef.current;
    if (!h || h.phase !== "question") return;
    const q = h.questions[h.qIndex];
    const ev = h.currentEvent;
    const effPts = ev && ev.id === "roulette" ? (h.rolled || 400) : q.pts;
    h.phase = "closing";
    const entries = Object.entries(h.answers)
      .filter(([, a]) => (a.text || "").trim())
      .map(([pid, a]) => ({ pid, text: a.text }));
    let okMap = {};
    if (q.exact) {
      entries.forEach((en) => { okMap[en.pid] = normNum(en.text) === normNum(q.a); });
    } else {
      h.judging = true;
      await broadcast();
      okMap = entries.length ? await judgeTyped(q.q, q.a, entries, cfg.src.ai, q.alt) : {};
      h.judging = false;
    }

    // من وقع عليه "كنسل" تنلغى إجابته
    Object.keys(okMap).forEach((pid) => {
      if ((h.fx[pid] || []).includes("cancel")) okMap[pid] = false;
    });

    const correct = Object.entries(h.answers)
      .filter(([pid]) => okMap[pid])
      .sort((x, y) => (x[1].ms || 0) - (y[1].ms || 0));
    const fastestPid = correct.length ? correct[0][0] : null;

    const results = Object.entries(h.players).map(([pid, p]) => {
      const a = h.answers[pid];
      const ok = !!(a && okMap[pid]);
      let gained = 0;
      if (ok) {
        if (ev && ev.id === "sniper") {
          gained = pid === fastestPid ? effPts + Math.round(effPts / 2) : 0;
        } else if (ev && ev.id === "steal") {
          gained = effPts; // بونص السرعة هنا هو السرقة نفسها
        } else {
          gained = effPts + (pid === fastestPid ? Math.round(effPts / 2) : 0);
        }
        if (ev && ev.id === "double") gained *= 2;
        if ((h.fx[pid] || []).includes("tax")) gained = Math.round(gained / 2);
      } else if (a && ev && ev.id === "risk") {
        gained = -Math.round(effPts / 2);
      }
      p.score += gained;
      return { pid, name: p.name, ok, ms: a ? a.ms : null, gained, fastest: pid === fastestPid && ok, answered: !!a, text: a ? a.text : null };
    });

    // حدث السرقة: الأسرع الصح يسحب نص النقاط من أعلى لاعب غيره
    let stealInfo = null;
    if (ev && ev.id === "steal" && fastestPid) {
      const amount = Math.round(effPts / 2);
      const others = Object.entries(h.players).filter(([pid]) => pid !== fastestPid).sort((x, y) => y[1].score - x[1].score);
      if (others.length && amount > 0) {
        const [vicPid, vic] = others[0];
        vic.score -= amount;
        h.players[fastestPid].score += amount;
        const thiefRow = results.find((r) => r.pid === fastestPid);
        const vicRow = results.find((r) => r.pid === vicPid);
        if (thiefRow) thiefRow.gained += amount;
        if (vicRow) vicRow.gained -= amount;
        stealInfo = { thief: h.players[fastestPid].name, victim: vic.name, amount };
      }
    }

    // جائزة: الفائز بسؤال الحدث ياخذ آيتم تخريب
    let grantInfo = null;
    if (ev && fastestPid) {
      const got = grantItem(fastestPid);
      if (got) grantInfo = { name: h.players[fastestPid].name, item: got };
    }
    const usedFx = h.fx;
    h.fx = {}; // التأثيرات تنتهي بنهاية السؤال

    results.sort((x, y) => y.gained - x.gained || (x.ms || 1e9) - (y.ms || 1e9));
    results.forEach((r) => { r.fx = usedFx[r.pid] || []; });
    h.stageStart = Date.now();
    h.reveal = {
      grant: grantInfo,
      correctText: q.a, qText: q.q, results, pts: effPts, event: ev || null,
      rolled: ev && ev.id === "roulette" ? effPts : null, steal: stealInfo,
    };
    h.phase = "reveal";
    await broadcast();
    rerender();
  }

  /* ---------- الهوست: التنقل ---------- */
  function pickEvent(h) {
    if (!h.eventPool || !h.eventPool.length) h.eventPool = shuffle(EVENTS.map((e) => e.id));
    const id = h.eventPool.pop();
    return EVENTS.find((e) => e.id === id);
  }

  async function beginQuestion(idx) {
    const h = hostRef.current;
    h.qIndex = idx;
    h.answers = {};
    h.qStart = Date.now() + START_DELAY; // الكل يبدأ بنفس اللحظة
    h.phase = "question";
    h.reveal = null;
    localRef.current = { qIndex: idx, renderAt: h.qStart, answered: false, text: "" };
    setTypedText("");
    await broadcast();
    rerender();
  }

  async function advanceTo(idx) {
    const h = hostRef.current;
    if (!h) return;
    h.reveal = null;
    h.currentEvent = null;
    if (idx >= h.total) {
      h.phase = "end";
      await broadcast();
      rerender();
      return;
    }
    h.qIndex = idx;
    h.catMode = idx === 0 ? "vote" : "pick";
    if (idx > 0) {
      const sorted = Object.entries(h.players).sort((a, b) => a[1].score - b[1].score);
      h.pickerPid = sorted.length ? sorted[0][0] : null; // الأقل نقاط
    } else {
      h.pickerPid = null;
    }
    h.catOptions = sampleCats();
    h.votes = {};
    h.catStart = Date.now();
    h.chosenCat = null;
    h.itemLog = [];
    Object.keys(h.players).forEach((pid) => { if (!h.items[pid]) h.items[pid] = []; });
    h.phase = "catpick";
    await broadcast();
    rerender();
  }

  async function startGame() {
    const h = hostRef.current;
    if (!h) return;
    Object.values(h.players).forEach((p) => { p.score = 0; });
    h.eventPool = null;
    h.used = new Set();
    h.questions = [];
    h.items = {}; h.fx = {}; h.itemLog = [];
    Object.keys(h.players).forEach((pid) => { h.items[pid] = []; });
    await advanceTo(0);
  }

  async function playAgain() {
    const h = hostRef.current;
    h.phase = "lobby";
    h.qIndex = 0;
    h.reveal = null;
    h.currentEvent = null;
    h.eventPool = null;
    h.used = new Set();
    h.questions = [];
    h.items = {}; h.fx = {}; h.itemLog = [];
    Object.keys(h.players).forEach((pid) => { h.items[pid] = []; });
    Object.values(h.players).forEach((p) => { p.score = 0; });
    await broadcast();
    rerender();
  }

  /* ---------- التصويت / الاختيار ---------- */
  async function castVote(cat) {
    setMyVote(cat);
    if (role === "host") {
      const h = hostRef.current;
      if (!h || h.phase !== "catpick") return;
      if (h.catMode === "pick") {
        if (h.pickerPid === me.pid) await resolveCat(cat);
      } else {
        h.votes[me.pid] = cat;
      }
    } else {
      await pushSlot({ vote: { q: view.qIndex, cat } }, true);
    }
  }

  /* ---------- إنشاء روم ---------- */
  async function createRoom() {
    if (!me.name.trim()) { setToast("اكتب اسمك أول"); return; }
    setLoading("نجهّز الروم…");
    await jset("fz:me", me, false);
    const c = genCode();
    // تنظيف أي خانات قديمة لنفس الرمز
    for (let s = 1; s <= MAX_SLOTS; s++) { try { await window.storage.delete(slotKey(c, s), true); } catch (e) { /* ما فيه */ } }
    hostRef.current = {
      code: c, total: cfg.count, questions: [], used: new Set(), qIndex: 0, qStart: 0, phase: "lobby",
      players: { [me.pid]: { name: me.name.trim(), score: 0 } },
      answers: {}, votes: {}, reveal: null, judging: false, currentEvent: null, eventPool: null, ver: 0,
      items: { [me.pid]: [] }, fx: {}, itemLog: [], seen: {}, doneUses: {}, stageStart: 0, autoNext: cfg.autoNext !== false,
      catMode: "vote", pickerPid: null, catOptions: [], catStart: 0, chosenCat: null, rolled: 0,
    };
    setCode(c);
    setRole("host");
    setScreen("game");
    setLoading("");
    await broadcast();
  }

  /* ---------- انضمام لاعب ---------- */
  const [joinCode, setJoinCode] = useState("");
  async function joinRoom() {
    if (!me.name.trim()) { setToast("اكتب اسمك أول"); return; }
    const c = joinCode.trim().toUpperCase();
    if (c.length !== 4) { setToast("الرمز 4 حروف"); return; }
    setLoading("ندوّر الروم…");
    let st = null;
    for (let i = 0; i < 3 && !st; i++) {
      if (i) await new Promise((r) => setTimeout(r, 900));
      st = await jget(roomKey(c));
    }
    setLoading("");
    if (!st) {
      setToast("ما لقينا روم «" + c + "» — تأكدون إنكم على نفس الرابط بالضبط");
      return;
    }
    await jset("fz:me", me, false);
    setLoading("نحجز لك مكان…");
    const s = await claimSlot(c);
    setLoading("");
    if (!s) { setToast("الروم ممتلئ (الحد " + MAX_SLOTS + " لاعبين)"); return; }
    mySlotRef.current = s;
    setCode(c);
    setRole("player");
    lastVerRef.current = st.v || 0;
    setView(st);
    setScreen("game");
  }

  // حجز خانة: نجرب الخانات بالترتيب ونتأكد إنها صارت لنا فعلاً
  async function claimSlot(c) {
    for (let s = 1; s <= MAX_SLOTS; s++) {
      const cur = await jget(slotKey(c, s));
      const free = !cur || !cur.pid || cur.pid === me.pid || Date.now() - (cur.t || 0) > 120000;
      if (!free) continue;
      await jset(slotKey(c, s), { pid: me.pid, name: me.name.trim(), t: Date.now() });
      await new Promise((r) => setTimeout(r, 350));
      const back = await jget(slotKey(c, s));
      if (back && back.pid === me.pid) return s;  // تأكدنا إنها لنا
    }
    return null;
  }

  // كتابة حالة اللاعب كاملة في خانته (اسم + إجابة + تصويت + آيتم)
  async function pushSlot(patch, critical) {
    const s = mySlotRef.current;
    if (!s) return false;
    slotDataRef.current = { ...slotDataRef.current, ...patch, pid: me.pid, name: me.name, t: Date.now() };
    let ok = await jset(slotKey(code, s), slotDataRef.current);
    // الإجابات والأصوات مهمة: نعيد المحاولة لو فشلت الكتابة
    if (!ok && critical) {
      for (let i = 0; i < 3 && !ok; i++) {
        await new Promise((r) => setTimeout(r, 500 * (i + 1)));
        slotDataRef.current.t = Date.now();
        ok = await jset(slotKey(code, s), slotDataRef.current);
      }
    }
    return ok;
  }

  /* ---------- اللاعب: مزامنة ---------- */
  useEffect(() => {
    if (role !== "player" || screen !== "game") return;
    let beat = 0;
    let timer = null;
    const loop = async () => {
      // نبض حضور: نعيد كتابة مفتاحنا كل ~6 ثواني تحسبًا لتأخر المزامنة
      beat++;
      if (beat % 10 === 1) pushSlot({}); // نبض حضور كل ~15 ثانية (ترشيد الطلبات)
      const t0 = Date.now();
      const st = await jget(roomKey(code));
      if (st && (st.v || 0) >= lastVerRef.current) {
        lastVerRef.current = st.v || 0;
        // نقدّر فرق الساعة: نأخذ أقل قيمة مقاسة (أقل تأخير شبكة)
        if (st.updatedAt) {
          const rtt = Date.now() - t0;
          const off = t0 + rtt / 2 - st.updatedAt;
          const arr = offSamplesRef.current;
          arr.push(off);
          if (arr.length > 40) arr.shift();
          // أدق عيّنة = أقلها (أقل تأخير تخزين)، وناخذ متوسط أفضل 5 لتقليل الضجيج
          const best = [...arr].sort((a, b) => a - b).slice(0, 5);
          offsetRef.current = best.reduce((s, x) => s + x, 0) / best.length;
        }
        setView(st);
      }
      // وتيرة متغيرة: سريعة بمراحل الانتظار (عشان يوصل السؤال بلا تأخير)، أهدأ أثناء السؤال
      const ph = viewRef.current && viewRef.current.phase;
      const gap = ["lobby", "catpick", "prep", "event"].includes(ph) ? 550 : 1500;
      timer = setTimeout(loop, gap);
    };
    loop();
    return () => clearTimeout(timer);
  }, [role, screen, code]);

  useEffect(() => { viewRef.current = view; }, [view]);

  useEffect(() => {
    if (!view) return;
    if (view.phase === "question" && localRef.current.qIndex !== view.qIndex) {
      const official = view.qStartAt ? view.qStartAt + (role === "host" ? 0 : offsetRef.current) : Date.now();
      // لو وصلك السؤال متأخر، ما تنعاقب: وقتك يبدأ من لحظة وصوله لك
      const startLocal = Math.max(official, Date.now());
      localRef.current = { qIndex: view.qIndex, renderAt: startLocal, answered: false, text: "" };
      setTypedText("");
    }
    if (view.phase === "catpick" && lastCatQRef.current !== view.qIndex) {
      lastCatQRef.current = view.qIndex;
      setMyVote(null);
    }
  }, [view && view.phase, view && view.qIndex]);

  /* ---------- إرسال الإجابة ---------- */
  async function answerTyped() {
    if (localRef.current.answered || !typedText.trim()) return;
    if (Date.now() < localRef.current.renderAt) return; // ما بدأ السؤال بعد
    const ms = Date.now() - localRef.current.renderAt;
    localRef.current.answered = true;
    localRef.current.text = typedText.trim();
    rerender();
    const reopen = () => { localRef.current.answered = false; rerender(); };
    if (role === "host") {
      const h = hostRef.current;
      if (h && h.phase === "question" && !(me.pid in h.answers)) h.answers[me.pid] = { text: typedText.trim(), ms };
    } else {
      const sent = await pushSlot({ ans: { q: view.qIndex, text: typedText.trim(), ms } }, true);
      if (!sent) { reopen(); setToast("تعذّر إرسال إجابتك — اضغط أرسل مرة ثانية"); }
    }
  }

  /* ---------- محرر أسئلتي ---------- */
  const [ed, setEd] = useState({ q: "", a: "", alt: "", d: 2, cat: "عام" });
  async function saveMyQuestion() {
    if (!ed.q.trim()) { setToast("اكتب نص السؤال"); return; }
    if (!ed.a.trim()) { setToast("اكتب الإجابة النموذجية"); return; }
    const item = {
      type: "typed", q: ed.q.trim(), a: ed.a.trim(), d: ed.d, cat: ed.cat,
      alt: ed.alt.split(/[,،]/).map((x) => x.trim()).filter(Boolean),
    };
    const next = [...myq, item];
    setMyq(next);
    await jset("fz:myq", next, false);
    setEd({ q: "", a: "", alt: "", d: 2, cat: ed.cat });
    setToast("انحفظ السؤال ✓");
  }
  async function deleteMyQuestion(i) {
    const next = myq.filter((_, idx) => idx !== i);
    setMyq(next);
    await jset("fz:myq", next, false);
  }

  /* ============================ الواجهات ============================ */

  const css = `
    @import url('https://fonts.googleapis.com/css2?family=Lalezar&family=Rubik:wght@400;500;700&display=swap');
    :root{
      --bg:#17121F; --sur:#241C31; --sur2:#2E2440; --line:#3D3153;
      --sand:#F3EADA; --dim:#9C90B0; --red:#D9494F; --amber:#F0A32F; --teal:#2EC4A6;
    }
    .fz *{box-sizing:border-box; margin:0; padding:0;}
    .fz{min-height:100vh; background:var(--bg); color:var(--sand); font-family:'Rubik',sans-serif; direction:rtl;}
    .wrap{max-width:520px; margin:0 auto; padding:20px 16px 48px; animation:fadeUp .28s ease both;}
    @keyframes fadeUp{from{opacity:0; transform:translateY(10px);}to{opacity:1; transform:none;}}
    .disp{font-family:'Lalezar',cursive; font-weight:400; letter-spacing:.5px;}
    .logo{font-family:'Lalezar',cursive; font-size:54px; line-height:1; text-align:center; color:var(--sand); direction:ltr; letter-spacing:2px;}
    .logo span{color:var(--amber);}
    .tag{text-align:center; color:var(--dim); margin-top:6px; font-size:15px;}
    .sadu{display:flex; flex-direction:column; gap:3px; margin:18px 0;}
    .zz{height:6px; opacity:.9;
      background:
        linear-gradient(-45deg, transparent 70%, var(--c) 70%) 0 0/12px 6px repeat-x,
        linear-gradient(45deg, transparent 70%, var(--c) 70%) 6px 0/12px 6px repeat-x;}
    .card{background:var(--sur); border:1px solid var(--line); border-radius:18px; padding:18px; margin-top:14px;}
    .btn{display:block; width:100%; border:none; border-radius:14px; padding:16px; font-size:18px; font-family:'Rubik',sans-serif;
      font-weight:700; cursor:pointer; transition:transform .12s ease, filter .15s ease; color:var(--bg);}
    .btn:active{transform:scale(.96);}
    .btn:focus-visible{outline:3px solid var(--amber); outline-offset:2px;}
    .btn-red{background:var(--red); color:#fff;}
    .btn-amber{background:var(--amber);}
    .btn-ghost{background:transparent; color:var(--sand); border:1.5px solid var(--line);}
    .btn:disabled{opacity:.45; cursor:default;}
    .inp{width:100%; background:var(--sur2); border:1.5px solid var(--line); border-radius:12px; padding:14px;
      color:var(--sand); font-size:17px; font-family:'Rubik',sans-serif; transition:border-color .15s ease;}
    .inp:focus{outline:none; border-color:var(--amber);}
    .lbl{display:block; color:var(--dim); font-size:14px; margin:14px 0 6px;}
    .chips{display:flex; flex-wrap:wrap; gap:8px;}
    .chip{border:1.5px solid var(--line); background:var(--sur2); color:var(--sand); border-radius:999px;
      padding:8px 14px; font-size:14px; cursor:pointer; font-family:'Rubik',sans-serif;
      transition:transform .12s ease, background .15s ease, border-color .15s ease;}
    .chip:active{transform:scale(.94);}
    .chip.on{background:var(--red); border-color:var(--red); color:#fff; font-weight:700;}
    .chip:focus-visible{outline:2px solid var(--amber); outline-offset:2px;}
    .seg{display:flex; gap:8px;}
    .seg .chip{flex:1; text-align:center;}
    .catGrid{display:grid; grid-template-columns:1fr 1fr; gap:10px; margin-top:14px;}
    .catBtn{border:1.5px solid var(--line); background:var(--sur); color:var(--sand); border-radius:14px;
      padding:18px 10px; font-size:16px; font-weight:700; cursor:pointer; font-family:'Rubik',sans-serif;
      transition:transform .12s ease, border-color .15s ease, background .15s ease;}
    .catBtn:active{transform:scale(.95);}
    .catBtn.on{background:var(--amber); border-color:var(--amber); color:var(--bg);}
    .catBtn:focus-visible{outline:3px solid var(--amber); outline-offset:2px;}
    .codebox{font-family:'Lalezar',cursive; font-size:56px; letter-spacing:12px; text-align:center;
      color:var(--amber); background:var(--sur2); border:2px dashed var(--amber); border-radius:16px; padding:8px 0 2px; direction:ltr;}
    .plist{display:flex; flex-direction:column; gap:8px; margin-top:10px;}
    .prow{display:flex; align-items:center; justify-content:space-between; background:var(--sur2);
      border-radius:12px; padding:12px 14px;}
    .ptsTile{font-family:'Lalezar',cursive; font-size:26px; color:var(--amber); background:var(--sur2);
      border:1.5px solid var(--line); border-radius:12px; padding:2px 14px 0; display:inline-block;}
    .qtext{font-size:22px; font-weight:700; line-height:1.6; margin:14px 0;}
    .fuseTrack{height:10px; background:var(--sur2); border-radius:999px; overflow:hidden; margin-top:14px;}
    .fuseBar{height:100%; background:linear-gradient(90deg, var(--amber), var(--red)); border-radius:999px;
      transition:width .12s linear; margin-inline-start:auto;}
    .meta{display:flex; align-items:center; justify-content:space-between; gap:8px;}
    .badge{font-size:13px; color:var(--dim);}
    .evBanner{display:flex; align-items:center; gap:10px; background:#3A2E1E; border:1.5px solid var(--amber);
      border-radius:14px; padding:10px 14px; margin-top:12px; font-weight:700; color:var(--amber);}
    .evSplash{text-align:center; padding:30px 0 10px;}
    .evIcon{font-size:72px; line-height:1;}
    .evName{font-family:'Lalezar',cursive; font-size:44px; color:var(--amber); margin-top:8px;}
    .evDesc{color:var(--sand); font-size:18px; margin-top:6px; line-height:1.7;}
    .rrow{display:flex; align-items:center; justify-content:space-between; background:var(--sur2);
      border-radius:12px; padding:12px 14px; margin-top:8px;}
    .rrow.ok{border-inline-start:4px solid var(--teal);}
    .rrow.bad{border-inline-start:4px solid var(--red); opacity:.85;}
    .gain{font-family:'Lalezar',cursive; font-size:22px;}
    .correctBox{background:#173330; border:1.5px solid var(--teal); color:var(--teal); border-radius:14px;
      padding:14px; font-size:18px; font-weight:700; text-align:center;}
    .stealBox{background:#33202E; border:1.5px solid var(--red); color:var(--sand); border-radius:14px;
      padding:12px; font-size:16px; font-weight:700; text-align:center; margin-top:10px;}
    .itemGrid{display:grid; grid-template-columns:1fr 1fr; gap:8px; margin-top:10px;}
    .itemBtn{display:flex; flex-direction:column; align-items:center; gap:4px; text-align:center;
      background:var(--sur2); border:1.5px solid var(--line); color:var(--sand); border-radius:14px;
      padding:12px 8px; cursor:pointer; font-family:'Rubik',sans-serif; font-size:13px;
      transition:transform .12s ease, border-color .15s ease;}
    .itemBtn:active{transform:scale(.94);}
    .itemBtn:hover{border-color:var(--amber);}
    .itemBtn:focus-visible{outline:3px solid var(--amber); outline-offset:2px;}
    .logRow{background:var(--sur2); border-radius:10px; padding:8px 12px; font-size:14px; margin-bottom:6px;}
    .fxBanner{display:flex; flex-wrap:wrap; gap:10px; justify-content:center; background:#33202E;
      border:1.5px solid var(--red); border-radius:14px; padding:10px; margin-top:12px;
      font-weight:700; font-size:14px; color:var(--sand);}
    .grantBox{background:#1E3320; border:1.5px solid var(--teal); color:var(--sand); border-radius:14px;
      padding:12px; font-size:16px; font-weight:700; text-align:center; margin-top:10px;}
    .voteTicks{display:block; color:var(--teal); font-size:15px; letter-spacing:2px; margin-top:4px;}
    .voterNames{display:block; color:var(--dim); font-size:11px; letter-spacing:0; font-weight:400; margin-top:2px;}
    .cdNum{font-family:'Lalezar',cursive; font-size:96px; line-height:1; color:var(--amber);
      animation:cdPop .5s cubic-bezier(.2,1.5,.4,1);}
    @keyframes cdPop{0%{transform:scale(.4); opacity:0;}60%{transform:scale(1.15);}100%{transform:scale(1); opacity:1;}}
    .warnBox{background:#3A2418; border:1.5px solid var(--red); color:var(--sand); border-radius:14px;
      padding:14px; font-size:15px; font-weight:700; margin-bottom:14px; text-align:right;}
    .hiddenQ{background:var(--sur2); border:1.5px dashed var(--line); border-radius:14px; padding:24px;
      text-align:center; font-size:20px; color:var(--dim); margin:14px 0;}
    .podium{display:flex; align-items:flex-end; justify-content:center; gap:10px; margin:22px 0 10px;}
    .pod{flex:1; max-width:120px; text-align:center;}
    .podBar{border-radius:12px 12px 0 0; display:flex; align-items:flex-start; justify-content:center;
      padding-top:8px; font-family:'Lalezar',cursive; font-size:28px; color:var(--bg);}
    .toast{position:fixed; bottom:20px; right:50%; transform:translateX(50%); background:var(--sand); color:var(--bg);
      border-radius:12px; padding:12px 20px; font-weight:700; z-index:50; box-shadow:0 8px 24px rgba(0,0,0,.4); animation:fadeUp .2s ease;}
    .overlay{position:fixed; inset:0; background:rgba(23,18,31,.92); display:flex; flex-direction:column; gap:16px;
      align-items:center; justify-content:center; z-index:40; color:var(--amber); font-size:20px; font-weight:700;}
    .spin{width:44px; height:44px; border:4px solid var(--sur2); border-top-color:var(--amber); border-radius:50%;
      animation:sp 1s linear infinite;}
    @keyframes sp{to{transform:rotate(360deg);}}
    .pulse{animation:pl 1.6s ease-in-out infinite;}
    @keyframes pl{0%,100%{opacity:1;}50%{opacity:.55;}}
    .pop{animation:pp .4s cubic-bezier(.2,1.4,.4,1);}
    @keyframes pp{0%{transform:scale(.6); opacity:0;}100%{transform:scale(1); opacity:1;}}
    .back{background:none; border:none; color:var(--dim); font-size:15px; cursor:pointer; font-family:'Rubik',sans-serif; padding:6px 0;}
    .qcount{font-family:'Lalezar',cursive; color:var(--dim); font-size:18px;}
    .flash{animation:fl .5s ease;}
    @keyframes fl{0%{background:#3A2E1E;}100%{background:var(--sur);}}
    .egg{position:fixed; right:10px; bottom:8px; background:none; border:none; color:var(--dim); opacity:.45;
      font-size:11px; cursor:pointer; font-family:'Rubik',sans-serif; z-index:30;}
    .egg:hover{opacity:.9;}
    select.inp{appearance:none;}
    @media (prefers-reduced-motion: reduce){ .fz *{animation:none!important; transition:none!important;} }
  `;

  /* ---------- الشاشات ---------- */
  const Home = () => (
    <div className="wrap">
      <div style={{ marginTop: 40 }}>
        <div className="logo">SICK <span>TRIVIA</span></div>
        <div className="tag">اكتب جوابك — الأسرع الصح ياخذها ⚡</div>
      </div>
      <Sadu />
      {storeOk === "unknown" && (
        <div className="warnBox" style={{ borderColor: "var(--amber)", background: "#3A2E1E" }}>
          ℹ️ ما قدرنا نتأكد من التخزين
          <div style={{ fontWeight: 400, fontSize: 13, marginTop: 8, lineHeight: 1.8 }}>
            جرّب عادي — غالبًا بيشتغل. ولو ما ظهر الروم:
            <br />تأكدوا إنكم على <b>نفس الرابط المنشور</b>، ولا تفتحونه بوضع التصفح الخفي.
          </div>
        </div>
      )}
      <button className="btn btn-red" onClick={() => setScreen("setup")}>سوّ روم جديد</button>
      <div style={{ height: 10 }} />
      <button className="btn btn-amber" onClick={() => setScreen("join")}>ادخل روم</button>
      <div style={{ height: 10 }} />
      <button className="btn btn-ghost" onClick={() => setScreen("editor")}>أسئلتي ({myq.length})</button>
      <p style={{ color: "var(--dim)", fontSize: 13, textAlign: "center", marginTop: 24, lineHeight: 1.8 }}>
        أول سؤال بالتصويت، وبعدها الأخير بالترتيب يختار الفئة 😈
        <br />وكل 3 أسئلة حدث يقلب اللعبة 🎲
        <br />الفائز بسؤال الحدث ياخذ آيتم تخريب 🎒
      </p>
    </div>
  );

  const Setup = () => (
    <div className="wrap">
      <button className="back" onClick={() => setScreen("home")}>→ رجوع</button>
      <h2 className="disp" style={{ fontSize: 30 }}>إعداد الروم</h2>
      <Sadu />
      <label className="lbl">اسمك</label>
      <input className="inp" value={me.name} onChange={(e) => setMe({ ...me, name: e.target.value })} placeholder="مثلاً: حسين" />

      <label className="lbl">عدد الأسئلة</label>
      <div className="seg">
        {[9, 15, 20, 25].map((n) => (
          <button key={n} className={"chip" + (cfg.count === n ? " on" : "")} onClick={() => setCfg({ ...cfg, count: n })}>{n}</button>
        ))}
      </div>

      <label className="lbl">مصادر الأسئلة</label>
      <div className="chips">
        <button className={"chip" + (cfg.src.bank ? " on" : "")} onClick={() => setCfg({ ...cfg, src: { ...cfg.src, bank: !cfg.src.bank } })}>بنك الأسئلة ({BANK.length})</button>
        <button className={"chip" + (cfg.src.ai ? " on" : "")} onClick={() => setCfg({ ...cfg, src: { ...cfg.src, ai: !cfg.src.ai } })}>توليد AI</button>
        <button className={"chip" + (cfg.src.mine ? " on" : "")} onClick={() => setCfg({ ...cfg, src: { ...cfg.src, mine: !cfg.src.mine } })}>أسئلتي ({myq.length})</button>
      </div>

      <label className="lbl">إيقاع اللعبة</label>
      <div className="chips">
        <button className={"chip" + (cfg.autoNext !== false ? " on" : "")}
          onClick={() => setCfg({ ...cfg, autoNext: cfg.autoNext === false })}>
          {cfg.autoNext !== false ? "✓ " : ""}انتقال تلقائي بين الأسئلة
        </button>
      </div>
      <p style={{ color: "var(--dim)", fontSize: 13, marginTop: 12, lineHeight: 1.8 }}>
        الفئات ما تنحدد هنا — كل سؤال، الأخير بالترتيب يختار فئته من داخل اللعبة 😈
        <br />التصحيح يشتغل بدون إنترنت ويتسامح مع الأخطاء الإملائية. توليد AI اختياري ويشتغل من جهازك أنت بس — ربعك ما يحتاجون حساب.
      </p>

      <div style={{ height: 20 }} />
      <button className="btn btn-red" onClick={createRoom}>جهّز الروم</button>
    </div>
  );

  const Join = () => (
    <div className="wrap">
      <button className="back" onClick={() => setScreen("home")}>→ رجوع</button>
      <h2 className="disp" style={{ fontSize: 30 }}>ادخل روم</h2>
      <Sadu />
      <label className="lbl">اسمك</label>
      <input className="inp" value={me.name} onChange={(e) => setMe({ ...me, name: e.target.value })} placeholder="مثلاً: عبدالله" />
      <label className="lbl">رمز الروم</label>
      <input className="inp" style={{ textAlign: "center", letterSpacing: 8, fontSize: 24, direction: "ltr", textTransform: "uppercase" }}
        value={joinCode} maxLength={4} onChange={(e) => setJoinCode(e.target.value)} placeholder="ABCD" />
      <div style={{ height: 20 }} />
      <button className="btn btn-amber" onClick={joinRoom}>ادخل</button>
    </div>
  );

  const Editor = () => (
    <div className="wrap">
      <button className="back" onClick={() => setScreen("home")}>→ رجوع</button>
      <h2 className="disp" style={{ fontSize: 30 }}>أسئلتي</h2>
      <p style={{ color: "var(--dim)", fontSize: 13 }}>أسئلة كتابية — الذكاء الاصطناعي يحكم على الإجابات بمرونة</p>
      <Sadu />
      <div className="card">
        <label className="lbl">نص السؤال</label>
        <input className="inp" value={ed.q} onChange={(e) => setEd({ ...ed, q: e.target.value })} placeholder="اكتب السؤال…" />
        <label className="lbl">الإجابة النموذجية</label>
        <input className="inp" value={ed.a} onChange={(e) => setEd({ ...ed, a: e.target.value })} placeholder="كلمة أو كلمتين" />
        <label className="lbl">إجابات بديلة مقبولة (اختياري — افصل بفاصلة)</label>
        <input className="inp" value={ed.alt} onChange={(e) => setEd({ ...ed, alt: e.target.value })} placeholder="مثال: Malenia، ماليينيا" />
        <label className="lbl">الفئة</label>
        <select className="inp" value={ed.cat} onChange={(e) => setEd({ ...ed, cat: e.target.value })}>
          {CATS.filter((c) => c !== "رياضيات").map((c) => <option key={c} value={c}>{c}</option>)}
        </select>
        <label className="lbl">الصعوبة</label>
        <div className="seg">
          {[1, 2, 3].map((d) => (
            <button key={d} className={"chip" + (ed.d === d ? " on" : "")} onClick={() => setEd({ ...ed, d })}>
              {DLBL[d]} ({PTS[d]})
            </button>
          ))}
        </div>
        <div style={{ height: 14 }} />
        <button className="btn btn-amber" onClick={saveMyQuestion}>احفظ السؤال</button>
      </div>
      {myq.map((q, i) => (
        <div className="prow" key={i} style={{ marginTop: 8 }}>
          <div style={{ fontSize: 14, flex: 1, marginInlineEnd: 8 }}>
            <span className="ptsTile" style={{ fontSize: 16, padding: "0 8px", marginInlineEnd: 8 }}>{PTS[q.d]}</span>
            <span className="badge">{q.cat} · </span>{q.q}
          </div>
          <button className="chip" onClick={() => deleteMyQuestion(i)}>حذف</button>
        </div>
      ))}
    </div>
  );

  const Lobby = () => (
    <div className="wrap">
      <h2 className="disp" style={{ fontSize: 26, textAlign: "center" }}>رمز الروم</h2>
      <div className="codebox">{code}</div>
      <p style={{ color: "var(--dim)", fontSize: 13, textAlign: "center", marginTop: 10 }}>
        خل ربعك يفتحون <b>نفس الرابط</b> ويدخلون الرمز
      </p>
      <Sadu />
      <div className="card">
        <div className="meta">
          <b>اللاعبين ({view.board.length})</b>
          <span className="badge pulse">ننتظر البقية…</span>
        </div>
        {role === "host" && view.board.length < 2 && (
          <p className="badge" style={{ marginTop: 8, lineHeight: 1.7 }}>
            ما ظهر أحد بعد؟ تأكدوا إنكم على نفس الرابط المنشور، وخلهم يضغطون «ادخل روم» ويكتبون <b>{code}</b>. يظهرون خلال ثانيتين.
          </p>
        )}
        <div className="plist">
          {view.board.map((p) => (
            <div className="prow" key={p.pid}>
              <span>{p.name}{p.pid === me.pid ? " (أنت)" : ""}</span>
              <span style={{ color: "var(--teal)" }}>جاهز ✓</span>
            </div>
          ))}
        </div>
      </div>
      {role === "host" ? (
        <div style={{ marginTop: 16 }}>
          <button className="btn btn-red" onClick={startGame}>ابدأ اللعب ({view.total} سؤال)</button>
        </div>
      ) : (
        <p style={{ textAlign: "center", color: "var(--dim)", marginTop: 16 }} className="pulse">ننتظر الهوست يبدأ…</p>
      )}
    </div>
  );


  const ItemBar = () => {
    if (!view || !view.canUseItems) return null;
    const inv = (view.items && view.items[me.pid]) || [];
    const log = view.itemLog || [];
    if (!inv.length && !log.length) return null;
    const targets = view.board.filter((p) => p.pid !== me.pid);
    return (
      <div className="card" style={{ marginTop: 16 }}>
        {log.map((l, i) => (
          <div key={i} className="logRow">{l.icon} {l.text}</div>
        ))}
        {inv.length > 0 && (
          <div>
            <div className="meta" style={{ marginTop: log.length ? 12 : 0 }}>
              <b style={{ fontSize: 15 }}>🎒 آيتماتك ({inv.length})</b>
              {pendingItem && <button className="chip" onClick={() => setPendingItem(null)}>إلغاء</button>}
            </div>
            {!pendingItem ? (
              <div className="itemGrid">
                {inv.map((id, i) => {
                  const it = itemById(id);
                  if (!it) return null;
                  return (
                    <button key={i} className="itemBtn"
                      onClick={() => (it.target ? setPendingItem({ id, slot: i }) : useItem(id, null))}>
                      <span style={{ fontSize: 24 }}>{it.icon}</span>
                      <b>{it.name}</b>
                      <span className="badge" style={{ fontSize: 11, lineHeight: 1.5 }}>{it.desc}</span>
                    </button>
                  );
                })}
              </div>
            ) : (
              <div>
                <p className="badge" style={{ margin: "10px 0 6px" }}>
                  {itemById(pendingItem.id).icon} على مين ترميه؟
                </p>
                {targets.length ? (
                  <div className="chips">
                    {targets.map((p) => (
                      <button key={p.pid} className="chip"
                        onClick={() => { useItem(pendingItem.id, p.pid); setPendingItem(null); }}>{p.name}</button>
                    ))}
                  </div>
                ) : (
                  <p style={{ color: "var(--red)", fontSize: 14, fontWeight: 700 }}>
                    ما فيه لاعبين ثانيين ظاهرين — انتظر ثانيتين وجرّب
                  </p>
                )}
              </div>
            )}
          </div>
        )}
      </div>
    );
  };

  const CatPick = () => {
    const cm = view.cm;
    if (!cm) return null;
    const isVote = cm.mode === "vote";
    const iAmPicker = !isVote && cm.pickerPid === me.pid;
    const canChoose = isVote || iAmPicker;
    return (
      <div className="wrap">
        <div className="meta">
          <span className="qcount">سؤال {view.qIndex + 1} / {view.total}</span>
        </div>
        <div className="evSplash pop" style={{ padding: "16px 0 4px" }}>
          <div className="evIcon">{isVote ? "🗳️" : "😈"}</div>
          <div className="evName" style={{ fontSize: 34 }}>
            {isVote ? "صوّتوا للفئة!" : iAmPicker ? "أنت الأخير — اختر الفئة" : cm.pickerName + " يختار الفئة"}
          </div>
          <div className="evDesc" style={{ fontSize: 15, color: "var(--dim)" }}>
            {isVote ? "الأغلبية تحدد فئة أول سؤال" : iAmPicker ? "انتقم… اختر اللي تكسّر فيه 😈" : "الأقل نقاط له حق الاختيار"}
          </div>
        </div>
        {canChoose ? (
          <div className="catGrid">
            {cm.options.map((c) => {
              const names = (cm.voters && cm.voters[c]) || [];
              return (
                <button key={c} className={"catBtn" + (myVote === c ? " on" : "")} onClick={() => castVote(c)}>
                  <span>{c}</span>
                  {isVote && names.length > 0 && (
                    <span className="voteTicks">
                      {"✓".repeat(Math.min(names.length, 5))}
                      <span className="voterNames">{names.join(" · ")}</span>
                    </span>
                  )}
                </button>
              );
            })}
          </div>
        ) : (
          <div className="card" style={{ textAlign: "center" }}>
            <p className="pulse" style={{ color: "var(--dim)" }}>ننتظر الاختيار…</p>
          </div>
        )}
        {isVote && (
          <p style={{ textAlign: "center", color: myVote ? "var(--teal)" : "var(--dim)", marginTop: 14, fontWeight: 700 }}>
            صوّت {cm.voted || 0} من {cm.totalPlayers || 1}{myVote ? " · اخترت «" + myVote + "» ✓" : ""}
            {view.catStart ? " · " + Math.max(0, Math.ceil(VOTE_SEC - (Date.now() - view.catStart) / 1000)) + " ث" : ""}
          </p>
        )}
        {role === "host" && (
          <div style={{ marginTop: 14 }}>
            <button className="btn btn-ghost" onClick={() => resolveCat(myVote || cm.options[0])}>
              تخطّ وابدأ السؤال ←
            </button>
          </div>
        )}
        <ItemBar />
      </div>
    );
  };

  const Prep = () => (
    <div className="wrap">
      <div className="evSplash">
        <div className="spin" style={{ margin: "30px auto 16px" }} />
        <div className="evName" style={{ fontSize: 28 }}>نجهّز سؤال {view.chosenCat}…</div>
        <p className="badge" style={{ marginTop: 10 }}>ثانية وحدة بس</p>
      </div>
    </div>
  );

  const EventSplash = () => {
    const ev = view.event;
    if (!ev) return null;
    return (
      <div className="wrap">
        <div className="evSplash pop">
          <div className="evIcon">{ev.icon}</div>
          <div className="evName">{ev.name}</div>
          <div className="evDesc">{ev.desc}</div>
          {view.chosenCat && <div className="badge" style={{ marginTop: 10 }}>الفئة: {view.chosenCat}</div>}
        </div>
        <Sadu />
        <ItemBar />
        <div style={{ height: 14 }} />
        {(() => {
          const left = view.autoNext ? Math.max(0, Math.ceil(EVENT_SEC - (Date.now() - (view.stageStart || 0)) / 1000)) : null;
          return role === "host" ? (
            <button className="btn btn-red" onClick={() => beginQuestion(view.qIndex)}>
              يلا — ابدأ السؤال 🎲 {left !== null ? "(" + left + ")" : ""}
            </button>
          ) : (
            <p style={{ textAlign: "center", color: "var(--dim)" }} className="pulse">
              {left !== null ? `يبدأ بعد ${left} ث — استخدم آيتماتك!` : "استعد… الهوست بيبدأ السؤال"}
            </p>
          );
        })()}
      </div>
    );
  };

  const Question = () => {
    const q = view.q;
    if (!q) return null;
    const ev = view.event;
    const fx = (view.fx && view.fx[me.pid]) || [];
    const has = (id) => fx.includes(id);
    const preMs = localRef.current.renderAt - Date.now();
    const elapsed = (Date.now() - localRef.current.renderAt) / 1000;
    const myDur = has("rush") ? q.dur / 2 : q.dur;
    const remain = Math.max(0, myDur - elapsed);
    const answered = localRef.current.answered;
    const blindHidden = (ev && ev.id === "blind" && elapsed > BLIND_SHOW) || (has("vanish") && elapsed > 5);
    const isRoulette = ev && ev.id === "roulette";
    const locked = has("lock") && elapsed < 8;
    const cancelled = has("cancel");
    const foggy = has("fog") && elapsed < myDur / 2;
    return (
      <div className="wrap">
        <div className="meta">
          <span className="qcount">سؤال {view.qIndex + 1} / {view.total}</span>
          <span className="ptsTile">{isRoulette ? "؟؟" : ev && ev.id === "double" ? q.pts + "×2" : q.pts}</span>
        </div>
        <div className="meta" style={{ marginTop: 6 }}>
          <span className="badge">{q.cat} · {DLBL[q.d]}</span>
          <span className="badge">⏱ {preMs > 0 ? Math.round(myDur) : Math.floor(remain)} ث</span>
        </div>
        {ev && (
          <div className="evBanner"><span style={{ fontSize: 22 }}>{ev.icon}</span> {ev.name}: {ev.desc}</div>
        )}
        <div className="fuseTrack">
          {preMs <= 0 && (
            <div className="fuseBar" style={{ width: Math.max(0, Math.min(100, (remain / myDur) * 100)) + "%" }} />
          )}
        </div>
        {fx.length > 0 && (
          <div className="fxBanner">
            {fx.map((id) => { const it = itemById(id); return it ? <span key={id}>{it.icon} {it.name}</span> : null; })}
          </div>
        )}
        {preMs > 0 ? (
          <div className="card" style={{ textAlign: "center", padding: "40px 18px" }}>
            <div className="cdNum" key={Math.ceil(preMs / 1000)}>{Math.ceil(preMs / 1000)}</div>
            <p style={{ color: "var(--dim)", marginTop: 8 }}>استعدوا… الكل يبدأ بنفس اللحظة</p>
          </div>
        ) : (
        <div className="card flash" key={view.qIndex}>
          {cancelled ? (
            <div className="hiddenQ">🚫 انكنسل سؤالك! تفرّج على الباقي بس</div>
          ) : blindHidden ? (
            <div className="hiddenQ">👻 السؤال اختفى! اكتب من ذاكرتك</div>
          ) : (
            <p className="qtext" style={{
              filter: foggy ? "blur(5px)" : "none",
              transform: has("mirror") ? "scaleX(-1)" : "none",
              transition: "filter .3s ease",
            }}>{q.q}</p>
          )}
          <input className="inp" value={typedText} disabled={answered || remain <= 0 || locked || cancelled}
            onChange={(e) => setTypedText(e.target.value)}
            onKeyDown={(e) => { if (e.key === "Enter") answerTyped(); }}
            placeholder={cancelled ? "ما تقدر تجاوب 🚫" : locked ? `مقفول… ${Math.ceil(8 - elapsed)} ث` : "اكتب إجابتك — عربي أو إنجليزي"} />
          <div style={{ height: 10 }} />
          <button className="btn btn-amber" disabled={answered || remain <= 0 || locked || cancelled || !typedText.trim()} onClick={answerTyped}>أرسل ⚡</button>
        </div>
        )}
        {answered && <p className="pulse" style={{ textAlign: "center", color: "var(--teal)", marginTop: 12, fontWeight: 700 }}>وصلت إجابتك ✓ ننتظر البقية…</p>}
        {preMs <= 0 && (
          <div className="chips" style={{ justifyContent: "center", marginTop: 12 }}>
            {view.board.map((p) => {
              const done = (view.answeredPids || []).includes(p.pid);
              return (
                <span key={p.pid} className="chip" style={{
                  cursor: "default", opacity: done ? 1 : 0.5,
                  borderColor: done ? "var(--teal)" : "var(--line)",
                  color: done ? "var(--teal)" : "var(--dim)",
                }}>{done ? "✓ " : "… "}{p.name}</span>
              );
            })}
          </div>
        )}
        {!answered && remain <= 0 && preMs <= 0 && (
          <p className="pulse" style={{ textAlign: "center", color: "var(--red)", marginTop: 12, fontWeight: 700 }}>انتهى الوقت ⏱</p>
        )}
        {view.judging && <p className="pulse" style={{ textAlign: "center", color: "var(--amber)", marginTop: 12, fontWeight: 700 }}>نراجع الإجابات…</p>}
        {role === "host" && (
          <div style={{ marginTop: 16 }}>
            <button className="btn btn-ghost" onClick={closeQuestion}>اقفل الإجابات الحين</button>
          </div>
        )}
      </div>
    );
  };

  const Reveal = () => {
    const r = view.reveal;
    if (!r) return null;
    return (
      <div className="wrap">
        <div className="meta">
          <span className="qcount">سؤال {view.qIndex + 1} / {view.total}</span>
          <span className="ptsTile">{r.pts}</span>
        </div>
        {r.event && (
          <div className="evBanner">
            <span style={{ fontSize: 22 }}>{r.event.icon}</span> كان الحدث: {r.event.name}
            {r.rolled ? " — الروليت طلعت " + r.rolled + " نقطة!" : ""}
          </div>
        )}
        <div className="card">
          <p style={{ color: "var(--dim)", fontSize: 14 }}>{r.qText}</p>
          <div style={{ height: 10 }} />
          <div className="correctBox">الجواب: {r.correctText}</div>
          {r.steal && (
            <div className="stealBox">🦹 {r.steal.thief} سرق {r.steal.amount} نقطة من {r.steal.victim}!</div>
          )}
          {r.grant && (
            <div className="grantBox">🎁 {r.grant.name} كسب آيتم: {r.grant.item.icon} {r.grant.item.name}</div>
          )}
          {r.results.map((p) => (
            <div key={p.pid} className={"rrow " + (p.ok ? "ok" : "bad")}>
              <span>
                {p.fastest ? "⚡ " : ""}{p.name}
                {(p.fx || []).map((id) => { const it = itemById(id); return it ? <span key={id} title={it.name}> {it.icon}</span> : null; })}
                <span className="badge" style={{ marginInlineStart: 8 }}>
                  {p.answered ? (p.text ? "«" + p.text + "» · " : "") + (p.ms / 1000).toFixed(1) + " ث" : "ما جاوب"}
                </span>
              </span>
              <span className="gain" style={{ color: p.gained > 0 ? "var(--teal)" : p.gained < 0 ? "var(--red)" : "var(--dim)" }}>
                {p.gained > 0 ? "+" + p.gained : p.gained < 0 ? "−" + Math.abs(p.gained) : "0"}
              </span>
            </div>
          ))}
        </div>
        <div className="card">
          <b>الترتيب</b>
          <div className="plist">
            {view.board.map((p, i) => (
              <div className="prow" key={p.pid}>
                <span>{i === 0 ? "👑 " : (i + 1) + ". "}{p.name}{i === view.board.length - 1 && view.board.length > 1 ? " 😈" : ""}</span>
                <span className="ptsTile" style={{ fontSize: 18, padding: "0 10px" }}>{p.score}</span>
              </div>
            ))}
          </div>
          {view.board.length > 1 && (
            <p className="badge" style={{ marginTop: 10, textAlign: "center" }}>😈 = يختار فئة السؤال الجاي</p>
          )}
        </div>
        {(() => {
          const left = view.autoNext ? Math.max(0, Math.ceil(REVEAL_SEC - (Date.now() - (view.stageStart || 0)) / 1000)) : null;
          return role === "host" ? (
            <div style={{ marginTop: 14 }}>
              <button className="btn btn-red" onClick={() => advanceTo(view.qIndex + 1)}>
                {view.qIndex + 1 >= view.total ? "النتيجة النهائية 🏆" : `كمّل ← ${left !== null ? "(" + left + ")" : ""}`}
              </button>
            </div>
          ) : (
            <p style={{ textAlign: "center", color: "var(--dim)", marginTop: 14 }} className="pulse">
              {left !== null ? `السؤال الجاي بعد ${left} ث…` : "ننتظر الهوست…"}
            </p>
          );
        })()}
      </div>
    );
  };

  const End = () => {
    const b = view.board;
    const pod = [b[1], b[0], b[2]];
    const heights = [90, 130, 65];
    const colors = ["#B8B8C8", "var(--amber)", "#C98B4E"];
    const medals = ["🥈", "🥇", "🥉"];
    return (
      <div className="wrap">
        <h2 className="disp" style={{ fontSize: 34, textAlign: "center", marginTop: 20 }}>خلصت اللعبة!</h2>
        <Sadu />
        <div className="podium">
          {pod.map((p, i) => p ? (
            <div className="pod" key={p.pid}>
              <div style={{ fontSize: 28 }}>{medals[i]}</div>
              <div style={{ fontWeight: 700, fontSize: 15, marginBottom: 6, overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>{p.name}</div>
              <div className="podBar" style={{ height: heights[i], background: colors[i] }}>{p.score}</div>
            </div>
          ) : <div className="pod" key={i} />)}
        </div>
        {b.length > 3 && (
          <div className="card">
            {b.slice(3).map((p, i) => (
              <div className="prow" key={p.pid} style={{ marginTop: i ? 8 : 0 }}>
                <span>{i + 4}. {p.name}</span><span>{p.score}</span>
              </div>
            ))}
          </div>
        )}
        {role === "host" && (
          <div style={{ marginTop: 20 }}>
            <button className="btn btn-red" onClick={playAgain}>جولة ثانية 🔄</button>
          </div>
        )}
        <div style={{ height: 10 }} />
        <button className="btn btn-ghost" onClick={() => { setScreen("home"); setRole(null); setView(null); }}>الرئيسية</button>
      </div>
    );
  };

  return (
    <div className="fz">
      <style>{css}</style>
      {screen === "home" && Home()}
      {screen === "setup" && Setup()}
      {screen === "join" && Join()}
      {screen === "editor" && Editor()}
      {screen === "game" && view && (
        view.phase === "lobby" ? Lobby() :
        view.phase === "catpick" ? CatPick() :
        view.phase === "prep" ? Prep() :
        view.phase === "event" ? EventSplash() :
        view.phase === "question" || view.phase === "closing" ? Question() :
        view.phase === "reveal" ? Reveal() :
        view.phase === "end" ? End() : Lobby()
      )}
      {loading && (
        <div className="overlay"><div className="spin" /><div>{loading}</div></div>
      )}
      {toast && <div className="toast">{toast}</div>}
      <button className="egg" onClick={() => setToast("عم عبدالله يقول: بلن 😌")}>بلن عم عبدالله</button>
    </div>
  );
}
